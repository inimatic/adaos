# Интерпретация намерения в свободном тексте

## 1. **OVOS (OpenVoiceOS)** (Интегрирован)

OVOS — это форк Mycroft.ai, в котором ядро и стек NLU сделаны максимально модульными.
Для нас важны **два слоя**, которые можно переиспользовать напрямую.

### 🔹 Что взять из OVOS

| Компонент                       | Что делает                                                                | Как можно встроить в AdaOS                                                                                                             |
| ------------------------------- | ------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| **Padatious**                   | lightweight intent parser, использует шаблоны ("weather in {city}") и FST | Ты уже используешь адаптер — можно довести его до **feature parity**: Padatious умеет слоты, fuzzy-matching и контекстное наследование |
| **Adapt**                       | keyword-based intent parser                                               | можно добавить как fallback: «поиск ближайшего совпадения» (если Padatious и твои резолверы не сработали)                              |
| **OVOS NLU registry**           | централизованный загрузчик intent- и entity-спецификаций из yaml/json     | хорошо ложится на твой `NLURegistry`: можно унифицировать формат манифеста                                                             |
| **OVOS tokenizer и lang tools** | извлечение сущностей, fallback для языков без моделей                     | может заменить ручные regex (даты, города, числа, прилагательные)                                                                      |
| **ovos-lingua-franca**          | нормализация чисел, дат, времени для 20+ языков                           | позволит избавиться от собственных регулярных выражений для чисел и дат                                                                |

👉 Главное преимущество OVOS — всё написано на **Python** и **не требует внешнего сервера**. Можно включить как дополнительный backend в `adaos.services.nlu.adapters.ovos`.

---

## 🧩 2. **Rhasspy** (Интегрирован)

Rhasspy — это «офлайн-stack для голосового ассистента», модульная альтернатива Alexa/Google Home.
Он умеет гораздо больше, чем OVOS, но все модули отдельные и общаются через HTTP/MQTT.
Ты можешь использовать их изолированно.

### 🔹 Что взять из Rhasspy

| Компонент                          | Что делает                                                         | Как можно встроить в AdaOS                                                          |
| ---------------------------------- | ------------------------------------------------------------------ | ----------------------------------------------------------------------------------- |
| **Rhasspy NLU**                    | умеет работать с Rasa, Snips, Fsticuffs                            | можно использовать как внешний NLU endpoint (HTTP) при dev-режиме или офлайн-режиме |
| **Rhasspy fuzzywuzzy / Pronounce** | сопоставление звуковых и текстовых форм                            | полезно для мультиязычности и устных запросов                                       |
| **Entity Extractors**              | готовые классы для извлечения `datetime`, `location`, `number`     | можно импортировать как альтернативу собственным regex-модулям                      |
| **Rhasspy Intent Graph**           | умеет строить деревья намерений                                    | это уже почти то, что тебе нужно для `arbiter`: выбор намерения по контексту        |
| **MQTT-шина событий**              | можно использовать как reference для твоего event-bus между skills |                                                                                     |

Rhasspy хорош тем, что **всё разбито на сервисы** — ты можешь поднимать только `rhasspy-nlu-hermes` или `rhasspy-asr`, не трогая остальное.

---

## 🚀 3. **Практический сценарий для AdaOS**

Предлагаю интеграцию по уровням зрелости:

| Этап                                 | Цель                                                                     | Что интегрировать                                                                                          |
| ------------------------------------ | ------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------- |
| **Stage 1: адаптеры**                | Повысить качество извлечения сущностей без потери производительности     | `ovos-lingua-franca`, `pymorphy3`, Rhasspy `fuzzy_entity_extractor`                                        |
| **Stage 2: гибрид NLU**              | Разрешить конфликты между Padatious, Adapt и Regex                       | объединить их в едином `arbiter` как конкурирующие backend-модели                                          |
| **Stage 3: внешний NLU режим**       | Позволить hub/dev-нодам обращаться к `Rhasspy NLU` через REST            | добавить `--nlu-endpoint` в CLI (`adaos api serve --nlu-endpoint http://rhasspy:12101/api/text-to-intent`) |
| **Stage 4: распределённый NLU**      | Каждый узел (hub/member) может подгружать языковые модели и делиться ими | использовать формат Rhasspy profiles для конфигурации локальных моделей                                    |
| **Stage 5: семантическая агрегация** | Комбинировать твои декларативные NLU-манифесты с обучаемыми сетями       | подключить Rasa или HuggingFace модель через Rhasspy backend                                               |

---

## 💡 Почему это рационально

* Ты **экономишь месяцы** на лексических деталях (падежи, даты, формат времени).
* Всё open-source и совместимо по лицензиям (MIT/GPL3).
* Работает офлайн — важно для приватности.
* Легко sandbox'ится: Python-модули для OVOS, контейнеры для Rhasspy.

---

## 🧠 Резюме

| Цель                                           | Что выбрать                                      |
| ---------------------------------------------- | ------------------------------------------------ |
| Улучшить извлечение чисел, дат, локаций        | **OVOS lingua-franca**, Rhasspy entity extractor |
| Добавить fallback-парсер без ML                | **Adapt** (из OVOS)                              |
| Получить готовую архитектуру intent-resolution | **Rhasspy NLU**                                  |
| Поддерживать мультиязычность                   | **OVOS** имеет инфраструктуру локалей            |
| Реализовать распределённый NLU                 | **Rhasspy Hermes/MQTT** как образец              |

# СОпоставление решений

## Rhasspy NLU (как единый бекенд)

**Плюсы**

* Единая точка: поддержка Rasa, Snips, Fsticuffs, regex; REST API (text-to-intent).
* Богатые энтити-экстракторы, граф намерений, профили языков.
* Быстро накрывает RU/EN мультиязычность и сложные примеры.

**Минусы**

* Отдельный сервис (ресурсы, деплой, лайфцикл, наблюдение).
* Сетевой hop → +латентность; деградация при недоступности.
* Шаблоны/слоты хранятся “снаружи” — сложнее делать ваш манифест как single source of truth.

## OVOS (локальные компоненты)

**Плюсы**

* **Padatious**: шаблонный парсер с слотами, лёгкий и быстрый в процессе.
* **Adapt**: keyword-fallback (кандидаты почти всегда есть).
* **lingua-franca**: числа/даты/время для 20+ языков (минус ваши regex).
* Всё — питон-модули, без отдельного демона; малыe задержки; офлайн.

**Минусы**

* Не ML/нейросети; для «свободной речи» покрытие хуже, чем у Rasa.
* Нужно связать с вашим реестром (вы уже почти сделали).

# Рекомендация по архитектуре

### Вариант A — Rhasspy-only (минимум компонентов)

* `services/nlu/adapters/rhasspy_client.py`: клиент `POST /api/text-to-intent`.
* `arbiter`: принимает кандидатов из Rhasspy, поверх — ваши **disambiguation** и контекст.
* `registry`: экспортирует ваш `manifest.intent` в формат Rhasspy (генерация sentences).
* **Плюс:** наименее кодозатратно.
* **Минус:** зависимость от сервиса и его доступности.

### Вариант B — Гибрид (практичный “best of both”)

* **Primary локально:** OVOS Padatious (+ Adapt fallback, + lingua-franca для дат/чисел).
* **Secondary внешне:** Rhasspy NLU как “умный fallback”, включаемый флагом.
* `arbiter` ранжирует: Padatious/Adapt → Rhasspy → regex (край).
* `registry` остаётся источником шаблонов/слотов (SST → Padatious, export → Rhasspy).
* **Плюс:** быстрый ответ офлайн, устойчивость, а сложные фразы тянет Rhasspy.
* **Минус:** чуть больше клея, но вы уже большую часть сделали.

# Когда что выбирать

* **Мобильный/встроенный офлайн, низкая латентность:** берите **OVOS + ваши резолверы**; Rhasspy опционален.
* **Быстро закрыть максимум кейсов без доп. кода:** **Rhasspy-only**.
* **Производство/“и то и это”:** **Гибрид** (OVOS локально, Rhasspy как fallback/расширение).

# Что делать дальше (конкретно)

1. **Интерфейс бекендов в одном месте**

   * `services/nlu/backends/base.py`: `parse(text, lang) -> [candidates]`.
   * Реализации: `ovos_backend.py`, `rhasspy_backend.py`, `regex_backend.py`.

2. **Arbiter-оркестратор**

   * Порядок: OVOS → Rhasspy (если включён) → regex.
   * Сшивка с вашим `registry` (utterances/slots/disambiguation) уже есть.

3. **Экспорт в Rhasspy**

   * Из `manifest.intent` генерить Rhasspy sentences; класть в профиль Rhasspy (или отдавать по API).

4. **Entity-стек**

   * Подменить ваши regex дат/чисел → **lingua-franca** (+ `pymorphy3` для RU лемматизации локаций).
   * Оставить `resolve_location` чистым и детерминированным (как сейчас), кормить `location_map` из реестра.

5. **Флаги/профили**

   * `ADAOS_NLU=ovos|rhasspy|hybrid` (+ таймауты/ретраи для внешнего бекенда).

# Интегрированное решение

Сначала **добиваем OVOS**, потом цепляем Rhasspy и уже затем LLM. вот план, который стартует именно с полного покрытия возможностей OVOS в вашей архитектуре.

# Этап A — доводим OVOS до «полной» интеграции

## A0. Базовая совместимость и фичефлаги

* `nlp.backends = ["ovos"]` по умолчанию.
* единый интерфейс `NLUBackend` уже есть/будет — OVOS остаётся первой реализацией.
* метрики/логи для OVOS: `nlu_ovos_latency_ms`, `nlu_ovos_score`, `nlu_ovos_errors_total`.

**PR**

* `core/nlu/backends/ovos.py` (актуализация)
* `config/defaults.yaml` (+ пороги/флаги OVOS)
* `docs/nlu/ovos.md` (1 стр. что включено)

---

## A1. Полный стек распознавания OVOS (Padatious + Adapt)

**цель:** получить лучшее покрытие за счёт двух механизмов и корректных score.

1. **Padatious**

* компиляция паттернов из `skill.yaml -> nlu.intents[*].utterances` (+ slotы).
* кэш артефактов тренировки на диск: ключ `(skill_id, skill_version, locale)`.
* hot-reload: пересборка при изменении манифеста навыка; TTL + дебаунс.
* нормализация score к [0..1].

2. **Adapt**

* генерация словарей из `entities`/`synonyms`/`vocab` в манифестах.
* лемматизация/стемминг ru/en перед подстановкой (pymorphy2 / simplemma).
* intent keywords + optional regex-entities (из `skill.yaml:nlu.entities[*].pattern`).

3. **Агрегация кандидатов**

* объединяем кандидатов Padatious/Adapt; политика: `max(score)` + тай-брейк по «specificity» (число заполненных слотов) и «pattern match depth».
* единый формат `Candidate(intent, slots, score, source="ovos/adapt/padatious")`.

**PR**

* `core/nlu/backends/ovos_padatious.py`
* `core/nlu/backends/ovos_adapt.py`
* `core/nlu/ovos_aggregate.py`
* `tests/nlu/test_ovos_{padatious,adapt,aggregate}.py`

---

## A2. Слоты и нормализация (lingua-franca)

**цель:** гарантированно «чистые» значения в слотах без костылей в навыках.

* `datetime/date/time`: через `ovos-lingua-franca` (locale-aware, ru/en), на выход — ISO (`date`, `time`, `datetime`, `tz` if present).
* `number/ordinal/amount`: числительные словами → числа; поддержка «тыс/млн», дробей и диапозонов.
* `duration`: `PT#H#M#S`.
* `measure`: число + единица (нормализация единиц).
* `location`: строка «как сказано» + леммы; последующий `resolve_location()` остаётся в ядре.
* `enum/boolean/freeform`: валидация по схеме навыка.

**PR**

* `core/nlu/slot_normalizer.py`
* `tests/nlu/test_slot_normalizer_ru_en.py`

---

## A3. Локали и морфология (RU/EN, код-свичинг)

**цель:** устойчивость к падежам/формам и смешанной речи.

* детектор языка на уровне запроса (fasttext-lite/char-heuristics); если смесь — делим на фразы.
* для ru: лемматизация (pymorphy2) перед Adapt, обратная сборка слотов.
* мэппинг локали: `ru-RU`, `en-US` → профиль правил lingua-franca.
* тесты на «в лондоне/из лондона/лондоне» и пр.

**PR**

* `core/nlu/lang_detect.py`
* обновление `ovos_adapt.py` (леммы в keywords)
* `tests/nlu/test_lang_mix_and_cases.py`

---

## A4. Фоллбеки и «договорки» OVOS внутри арбитра

**цель:** не потерять сильные стороны OVOS при конфликте кандидатов.

* поддержка «fallback priority» OVOS как дополнительного веса (например, `source_weight`).
* «спросите ещё» (disambiguation) — если два кандидата ~равны: арбитр возвращает follow-up с минимальным уточнением (слот/интент).
* короткие «да/нет», «а ещё…» — мини-сегментация: сначала базовый интент, потом добавочные слоты; контекст хранится в `nlu_context`.

**PR**

* `core/nlu/arbiter.py` (веса источников + follow-up API)
* `tests/nlu/test_disambiguation_followups.py`

---

## A5. Динамический словарь и синонимы из навыков

**цель:** навыки сами расширяют словари «на лету».

* из `skill.yaml:nlu.entities[*].values/synonyms` формируем Adapt vocab.
* Runtime API для навыков: `POST /nlu/vocab/update` (skill_id, locale, additions/removals).
* TTL и персист в локальный кэш; hot-reload Adapt.

**PR**

* `core/nlu/vocab_store.py`
* `apps/api/nlu_endpoints.py`
* `tests/nlu/test_dynamic_vocab.py`

---

## A6. Наблюдаемость и регрессия для OVOS

**цель:** контроль качества до подключения следующего бэкенда.

* метрики: распределение интентов, доля пустых матчей, средний score по источникам, время компиляции Padatious.
* dev-режим: писать `utterance → выбранный intent/slots/source/score` в «эталонный» журнал.
* CLI: `adaos nlu bench --backend ovos --suite tests/data/nlu_ru_en.jsonl`.

**PR**

* `tools/nlu_bench/`
* `docs/nlu/bench.md`
* `tests/data/nlu_ru_en.jsonl` (10–20 кейсов)

---

# Этап B — подключаем Rhasspy (после A)

* **B1.** Врезка Rhasspy NLU как грамматического усилителя (YAML/JSGF/regex-правила от навыков → адаптер).
* **B2.** Нормализация score и объединение с OVOS в арбитре.
* **B3.** Политика: Rhasspy звать при низком score OVOS **либо** если навык помечен `grammar_preferred: true`.

---

# Этап C — LLM как третий бэкенд (после A+B)

* аккуратно включаем как fallback, строго по JSON-схеме, с кэшем и circuit-breaker (как описывали ранее).

---

## Definition of Done для Этапа A (OVOS)

* ✅ При `nlp.backends=["ovos"]` покрываем намерения из `skill.yaml` через **Padatious+Adapt**, со слот-нормализацией.
* ✅ Кэш и hot-reload Padatious артефактов работают; словари Adapt собираются из манифестов и runtime API.
* ✅ RU/EN устойчивость: падежи/числа/даты/продолжительности проходят тесты.
* ✅ Метрики/логи/бенч CLI подключены; регрессионный набор зелёный.
