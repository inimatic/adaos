# Issue Tracker

Snapshot: 2026-08-19.

This document contains only active AdaOS execution work. Architecture,
sequencing, and milestone completion are owned by the documents listed in the
[Roadmap Inventory](architecture/roadmap-inventory.md). Completed task blocks
and dated investigation journals are removed from this file; recover them from
Git history or linked release evidence when needed.

Priority uses the shared planning vocabulary:

- `[must]`: blocks the current MVP or governed-evolution proof gate;
- `[should]`: required before broad or repeated use;
- `[could]`: useful but non-blocking;
- `[deferred]`: intentionally postponed until the stated condition.

Execution state is separate from priority: `open`, `in progress`, `queued`,
`ready for stand`, or `conditional`. A task leaves this tracker when its owner
accepts the required evidence; closed tasks do not remain as checked rows.

## Current Focus

The local Wave 0-1 gate is closed. The active sequence is:

1. prove the M1 projection path on the target stand;
2. build the M2 operator truth and durable operation plane;
3. land the shared M3 activation runtime and migrate the remaining heavy
   operator skills in an evidence-driven order selected from current demand,
   pressure, and stand failures;
4. finish browser/webspace acceptance and the release-candidate soak.

## Must

| ID | State | Work and next proof | Owner |
| --- | --- | --- | --- |
| `M2-TRUTH` / `STATUS-007` | in progress | The runtime/details split and event-boundary runtime probes removed periodic reliability polling from the browser hot path. Populate the remaining compact runtime/update/slot/route/Yjs/member/guard cards, keep control traffic inside its own budget, and promote the boundary probe to versioned push/delta consumption. | [MVP M2](mvp_roadmap.md#milestone-m2-operator-truth-plane), [Operational Event Model](architecture/operational-event-model-roadmap.md) |
| `M2-OPERATIONS` | ready for stand | Durable history, restart-to-`recoverable`, governed subprocess cancellation, and idempotent retry entry points are implemented. Prove recovery, duplicate-retry suppression, subprocess termination, and notifications through an API restart on a canary stand. | [MVP M2](mvp_roadmap.md#milestone-m2-operator-truth-plane), [Registry/Operations](architecture/registry-marketplace-operations-roadmap.md) |
| `M3-ACTIVATION` / `LRLT-005` | open | Add one activation runtime for loaded/active state, startup allowance, background refresh, and client presence; prove inactive lazy/on-demand skills remain cheap. | [MVP M3](mvp_roadmap.md#milestone-m3-activation-service-and-skill-migration-wave) |
| `M4-BROWSER` / `BSPH-001` / `MRI-001` | in progress | Keep source kinds explicit, render structure before deferred hydration, and make reload/resync recovery declarative. The `.30` `0.1.684` managed restart preserved an active control WS but produced no YWS reattach attempt within three minutes; prove provider reattach and first sync rather than treating server handoff readiness as browser recovery. | [MVP M4](mvp_roadmap.md#milestone-m4-webspace-and-browser-runtime-stabilization), [Webspace Evolution](architecture/webspace-evolution-roadmap.md) |
| `BROWSER-LIFECYCLE-001` | ready for stand | Deploy the bounded lifecycle snapshot recovery and reproduce a silent or dropped Root lifecycle SSE event while snapshot GET stays available. Prove `Recovering` lasts no longer than the expired lease plus one bounded GET, retries back off to at most 15 seconds, a later SSE event cancels polling, and browser diagnostics preserve reason, attempt, delay, and recovery duration. Correlate the browser evidence with node and Root logs before closure. | [Realtime Reliability](architecture/realtime-reliability-roadmap.md), [UI Runtime Diagnostics](architecture/ui-runtime-diagnostics.md) |
| `RCMS-006` | open | Persist hub/root catalog snapshots on members and prove no-git member drift, reconnect refresh, and archive materialization. | [Registry/Operations](architecture/registry-marketplace-operations-roadmap.md), [MVP M6](mvp_roadmap.md#milestone-m6-endpoint-and-device-reachability-matrix) |
| `RCMS-007` | open | Make inventory, lifecycle, scenario health, operation detail, and log access core-owned contracts shared by UI, API, and MCP. | [MVP M2](mvp_roadmap.md#milestone-m2-operator-truth-plane), [Registry/Operations](architecture/registry-marketplace-operations-roadmap.md) |
| `M6-IDENTITY` / `DIAU-001` / `DIAU-003` / `NER-002` | in progress | Prove canonical hub/browser/member identity, deterministic display names, and immediate or explicitly governed detach/logout behavior across local and routed topologies. | [MVP M6](mvp_roadmap.md#milestone-m6-endpoint-and-device-reachability-matrix), [Device Access Roadmap](architecture/device-access-roadmap.md) |
| `BUILDER-FORGE` | open | Make Root scenario-draft publication return durable Forge commit acknowledgement instead of `504` plus stale metadata. | [Builder Roadmap](architecture/builder-roadmap.md#phase-4-validation-and-preview), [Builder SDK Boundary](architecture/builder-sdk-boundary.md) |
| `BUILDER-DEV-VERSION-CACHE` | open | Reject different DEV skill bytes under an existing semantic version, or invalidate and refetch every local A/B cache by content digest. Until fixed, Builder acceptance must advance a unique version before activation. | [Builder Roadmap](architecture/builder-roadmap.md#phase-6-runtime-activation-and-rollback), [Skill Runtime Lifecycle](skill_runtime.md) |
| `M7-NEURAL-INSTALL` / `SFH-006` | open | On a clean workspace, install the Neural NLU provider without tracked runtime models and pass provider diagnostics plus dependency-profile checks. | [MVP M7](mvp_roadmap.md#milestone-m7-model-and-nlu-provider-baseline), [NLU Roadmap](architecture/nlu-roadmap.md) |
| `MVP-STAND-001` | in progress | The executable observe/browser evidence runner is implemented. Provision permanent browser access and secrets, prove one direct target-stand run, then wrap the same deterministic runner in the Root-owned validation-campaign and leased test-node flow. | [MVP M9](mvp_roadmap.md#milestone-m9-mvp-release-candidate-acceptance), [Post-Deploy E2E](architecture/post-deploy-e2e-testing.md) |
| `MVP-SOAK-001` / `HMG-005` / `STATUS-008` | queued | Run the bounded two-browser/root-routed soak with process-tree memory, pressure, operation, reconnect, quarantine, and residual-risk evidence. | [MVP M9](mvp_roadmap.md#milestone-m9-mvp-release-candidate-acceptance), [Realtime Reliability](architecture/realtime-reliability-roadmap.md) |

## Should

| ID | State | Work and next proof | Owner |
| --- | --- | --- | --- |
| `RT-ROUTE-INCIDENT-SCOPE-001` | ready for stand | A controlled `.30` cut of the single runtime-to-sidecar NATS socket recovered in 853 ms with the same runtime and skill PIDs and exactly one local/remote reconnect, but the old read model expanded it into eight route non-ready records: three per-session publish failures, one derived root outage, two stale-session `no_upstream` records, and two forced closes. The local fix separates state transitions from incidents, marks request/session/channel impact scope, preserves raw incident classes and counts, and restarts recovery hold only for real transitions or channel-impacting incidents. Deploy the batch, repeat the exact cut with an already-open rooted browser, and correlate node, sidecar, browser, and Root logs. Acceptance requires one bounded transport episode, no session-driven extension of browser `Recovering`, unchanged runtime/skill process identity, retained per-session evidence, and explicit agreement among transition, raw incident, and impacting-incident counters. Keep `RT-POST-INCIDENT-REVIEW-002` open for a later independent log review even if this campaign passes. | [Realtime Reliability](architecture/realtime-reliability-roadmap.md), [UI Runtime Diagnostics](architecture/ui-runtime-diagnostics.md) |
| `RT-FANOUT` / `SKILL-CHANNEL-001` / `LRLT-001` / `LRLT-002` / `HMG-001` / `HMG-002` / `HMG-006` | in progress | The `.30` watchdog attributed a real stall to `infrastate_skill -> skill_memory_get -> pathlib.read_text`; the 2026-08-15 cutover run added a second exact sample: `infrastate_skill 0.75.65 -> on_runtime_event(subnet.member.link.up)`, 5.443 s, with the watchdog frame in `importlib.get_data`. Core `a96c738d` now serializes post-boot migration behind handler import, keeps candidate migration deferred until promotion, limits each async skill handler to one active invocation, and opens an observable cooldown circuit after a severe over-budget call. Synchronous subscriptions remain bounded workers, and the registry audit reports zero known blocking paths. Redeploy the published skill releases, including `infrastate_skill 0.75.66`, prove the post-startup watchdog remains at zero stalls and no skill circuit opens, then prove a synthetic rejected skill and controlled Root outage cannot delay HTTP, `/ws`, `/yws`, or hub-root heartbeat recovery. Do not close from static proof alone. | [Realtime Reliability](architecture/realtime-reliability-roadmap.md), [Runtime Guarding](architecture/runtime-guarding.md), [Rebuild Lag Hardening](architecture/realtime-rebuild-lag-hardening.md) |
| `LRLT-004` | open | Reconfirm y-py thread affinity and safe diagnostics after the patched native-store/runtime changes. | [Runtime Guarding](architecture/runtime-guarding.md) |
| `LRLT-006` | open | Move long-running NLU/Teacher inference and apply work behind bounded workers while retaining cheap resolver/regex fast paths. | [NLU Roadmap](architecture/nlu-roadmap.md) |
| `UILOG-001` / `LRLT-007` | in progress | Add typed UI diagnostic ABI, duplicate suppression, rate/size limits, and post-restart skill-log retrieval acceptance. | [UI Runtime Diagnostics](architecture/ui-runtime-diagnostics.md) |
| `YJS-MATERIALIZATION-REPAIR` | ready for stand | The browser now separates transport sync from live-YDoc completeness and requests a bounded authoritative full-state repair without closing its provider. The Hub preserves the room, validates scenario authority, coalesces concurrent browser requests, reports update bytes and delivery counts, and leaves HTTP snapshots render-only. On `.30`, prove an intentionally incomplete client converges while the same YWS/WebRTC provider and page instance remain open, then hold a multi-browser soak without quadratic repair fanout. | [Hub-Browser Connectivity](architecture/hub-browser-connectivity.md) |
| `YJS-STARTUP-MATERIALIZATION-001` | ready for stand | The `.30` A/B update preserved a ready WS/YWS route but restarted with an empty process-local materialization status cache, so `desktop`, `homepoint`, and `desktop-dev` reported `status_cache_missing` until an operator reload. Startup now rebuilds every registered webspace status from its manifest and persisted YDoc through an isolated payload-only action; it does not reseed the scenario or mutate live branches, bounds each rebuild, continues after an individual failure, and records a per-webspace summary. Repeat an A/B restart without manual reload and prove all three webspaces report `ready` with the expected scenario/catalog/installed counts before the API is admitted, startup time stays bounded, the old runtime serves until candidate readiness, runtime/sidecar/service-skill ownership converges without unintended duplicate restarts, and no reload/reset event or extra NATS session is emitted. | [Webspace Evolution](architecture/webspace-evolution-roadmap.md), [UI Runtime Diagnostics](architecture/ui-runtime-diagnostics.md) |
| `DIAU-002` | open | Add modal/browser regression coverage for settings controls and destructive detach confirmation. | [Device Access Roadmap](architecture/device-access-roadmap.md) |
| `DIAU-004` | open | Finish the product-copy audit for device, browser, member, node, hub, endpoint, and subnet terms. | [Product Terminology](architecture/product-terminology.md) |
| `MRI-002` | open | Make workspace skill publication verifiable from source edit through targeted tests, push/version evidence, and compatible root/client checks. | [Registry/Operations](architecture/registry-marketplace-operations-roadmap.md) |
| `RCMS-001` | open | Make git requirements explicit by role and deployment mode, including member no-git archive materialization and operator diagnostics. | [Registry/Operations](architecture/registry-marketplace-operations-roadmap.md) |
| `RCMS-002` | in progress | Finish stale-catalog classification, explicit workspace fallback, row-level diagnostics, and scenario source/runtime actions. | [Registry/Operations](architecture/registry-marketplace-operations-roadmap.md) |
| `RCMS-003` / `SKILL-RUNTIME-ATOMIC-001` | ready for stand | Production refresh now preserves the active slot, stages same-version revisions in the inactive slot, asks the owner runtime to reload activated handlers, and exposes loaded-source and selected-slot drift. On `.30`, reproduce a same-version Weather update and prove the active digest remains unchanged during prepare, activation switches the slot atomically, `handler_reload.ok` is true, drift totals remain zero, and the selected city survives snapshot and reconnect. Then force one reload failure and verify the operation reports `restart_required` instead of silently claiming completion. | [Registry/Operations](architecture/registry-marketplace-operations-roadmap.md) |
| `CORE-ARTIFACT-CONVERGENCE-001` | in progress | The `91.98.89.76` core `0.1.876` post-boot migration reported `succeeded`, `total=0` and three quarantines while 19 installed skill runtimes were `runtime-behind`. The workspace had no `WorkspaceLock`; sparse placeholder directories existed without manifests, candidate selection treated directory existence as source authority, and post-boot migration disabled shared workspace sync. The first repaired `0.1.877` run correctly changed that result to `failed` on residual Builder drift, but exposed lost install intent: SQLite retained only 10 skills while 32 valid `current_runtime.json` selections existed, so shared sync kept the narrow sparse set and the audit enrolled only one row. Selected immutable runtimes are now part of install-intent authority and prevent reconcile from unregistering a live selection. Manual `infrastate_skill` migration also exposed a second defect: `skills.activated` was emitted before API handler reload, while the subprocess-only reload could not update the long-lived bus owner. The convergence path now falls back to registry versions for sparse placeholders, synchronizes the shared skill/scenario workspace when no lock exists, fails on residual drift, validates candidate handlers, reloads them in the owner before activation events, restores the exact prior A/B selection on live reload failure, and rebuilds the webspace once after the batch. A stand restart exposed a separate handoff defect: normal external `systemctl restart` preserved runtime children merely because self-restart was supported, left the unit in `final-sigterm` for the full 90-second stop timeout, and then forced systemd to kill the child. Child preservation is now gated only by an explicitly pending internal self-restart; ordinary shutdown stops runtime and sidecar. Repeat the stand analysis after deployment: prove all installed skills have zero runtime drift, required scenario manifests and dependency closure are materialized, Infra State leaves stale `0.1.800/restarting`, Weather/Subnet/Slideshow/Media surfaces use the new handlers, scenario/catalog materialization is fresh after desktop/Infrascope switch loops, an external restart completes without the 90-second timeout, and no migration claims success from `total=0` while registry/runtime versions differ. Preserve candidate/status JSON, loaded source/version/slot, event ordering, projection result, process/I/O pressure and channel latency for both a successful batch and an injected live-handler import failure. | [LLM Skill Development](guides/llm-skill-development.md), [Registry/Operations](architecture/registry-marketplace-operations-roadmap.md), [UI Runtime Diagnostics](architecture/ui-runtime-diagnostics.md) |
| `CORE-ARTIFACT-CONVERGENCE-STAND-001` | in progress | The repaired `0.1.877` path on `91.98.89.76` synchronized 32 selected skill manifests, enrolled 16 migration candidates, live-finalized 13 handlers in the bus owner, and correctly failed the batch instead of claiming success while four candidates remained behind. The residual causes were package defects rather than transport failures: three candidate suites assumed a source-monorepo path, while `slideshow_skill` declared a service runtime without explicitly admitting its core-owned event handlers. The fixed Builder `0.3.39`, Conversation `0.1.14`, CV Descriptor `0.1.3`, and Slideshow `0.1.67` candidates passed isolated install-strict validation and recovery activation; Infrascope `0.28.13` was reactivated with durable bounded worker diagnostics. During those five recoveries, `/api/ping` passed 184/184 samples (p95 51.5 ms, max 284.1 ms), and logs proved exact selected-slot handler reload plus Slideshow service replacement. A cold external restart completed in 4.1 seconds instead of hitting the former 90-second stop timeout; the API was unavailable for 20.3 seconds during bootstrap, then returned with one supervisor, runtime and sidecar, `ready/stable` root and browser routes, and all six checked skills still exact with zero version drift. Infra State now reads authoritative `succeeded`, `slot A | 0.1.877 | e8f5927`; neither `0.1.800` nor `restarting` remains in the fresh result. Keep this item open for a deployed, non-overlay repeat: classify stale/deactivated generated runtime selections separately from installed release drift, prove required scenario closure and desktop/Infrascope switch materialization, exercise an injected owner reload rollback, and measure whether the 20-second cold-start API gap can be reduced without admitting an incomplete runtime. | [LLM Skill Development](guides/llm-skill-development.md), [Registry/Operations](architecture/registry-marketplace-operations-roadmap.md), [Realtime Reliability](architecture/realtime-reliability-roadmap.md) |
| `RCMS-005` | open | Keep production CLI/control actions slot-bound and development commands explicitly rooted in `.adaos/dev`. | [Realtime Reliability](architecture/realtime-reliability-roadmap.md) |
| `HMG-007` | open | Preserve correlation/generation IDs and reject guard behavior that hides overload rather than reducing it. | [Runtime Guarding](architecture/runtime-guarding.md) |
| `ROOT-MCP-TARGET` / `F3M-006A` / `F3M-006C` | open | Keep selectors out of managed target IDs and separate direct remote MCP health from bearer validity with deployed-session evidence. | [Root MCP Roadmap](architecture/root-mcp-roadmap.md) |
| `ROOT-PUBLIC-SPLIT` / `F3M-006D` | open | Separate public bearer/bootstrap/Codex surfaces from mTLS API surfaces and add deploy smoke for both hosts. | [Security](architecture/security.md), [Root MCP Roadmap](architecture/root-mcp-roadmap.md) |
| `RT-DIAGNOSTICS` / `F3M-002` / `F3M-010` | ready for stand | The reproduced sidecar/direct-WSS incident now has identity-aware control-port readiness, protocol roundtrip liveness, bounded process/network-I/O lookback, and durable incident persistence. Prove on the target stand that readiness creates no NATS session and that an outbound-only direct WSS session is not recycled by raw-RX idleness. | [Realtime Reliability](architecture/realtime-reliability-roadmap.md) |
| `RT-POST-INCIDENT-001` / `LRLT-008` | in progress | `.805` is active on `.30` as `0.1.805+1.4103cd5`. Thirty supervisor-channel reads and a later 360 s soak left `local_client_total`, session open/close, remote connect/failure, quarantine, supersede, and overlap counters unchanged. The soak passed runtime `360/360`, sidecar readiness `360/360`, and supervisor-channel `177/177`; runtime ping p95 was 2.047 ms and sidecar p95 was 0.990 ms. An exact loopback NATS cut produced one local and one remote reconnect only; runtime detected the cut at `08:35:27.103Z`, connected at `08:35:27.825Z`, and restored subscriptions by `08:35:27.829Z`, while all 145 runtime probes passed. Root kept one browser `/ws/subnet` route exchanging frames and returned heartbeat `200`; backend, NATS, and reverse-proxy restart counts stayed zero. The retrospective quality window changed from `degraded/flapping` to `ready/stable` after its bounded five-minute incident window instead of recycling transport. No post-promotion watchdog stall was attributed to a skill. Candidate startup recorded one 2.94 s selector stall with `skill_candidates=[]`; the later 2.21 s lag at `08:50:14Z` was caused by this investigation attaching `strace` and must not be used as product attribution. The original 2026-08-13 Root logs remain unavailable, so the initial trigger still cannot be attributed absolutely. Closure still requires a human-observed browser recovery campaign after the pending diagnostic-cost and slideshow resource fixes are deployed together. | [Realtime Reliability](architecture/realtime-reliability-roadmap.md) |
| `RT-POST-INCIDENT-REVIEW-002` | open | After the final post-`.805` fault-and-soak evidence is preserved, perform a separate later review of logs and counter deltas. Re-check terminal versus transient close classification, hidden resume loops, competing route owners, stale readiness/cache use, event-loop or executor starvation, persistence backlogs, skill circuit or SDK I/O-guard activity, downloads/media work, sidecar generation mismatch, SQLite writer waits, and contradictions between browser, node, sidecar, and Root state. The 2026-08-18 `.30` checkpoint must remain in the comparison set: Root control `POST /v1/hub/control/report` timed out first at `09:10:20Z`, recovered slowly at `09:10:39Z`, and only then did the NATS watchdog close its five-hour session at `09:10:44Z`; no same-window large download was found, while SQLite write waits and durable registration slowed the recovery path. Compare the bounded pre-failure process/I/O window with a quiet baseline and explicitly account for large downloads or update copies. Verify a stale remote lifecycle lease cannot gate an active loopback WS/YWS route, beacon stale responses never outlive their bounded window, and the supervisor-channel endpoint produces neither a watchdog frame in config/SQLite nor side effects on NATS session counters. Reconfirm that a Windows venv launcher child is attributed to the managed sidecar lineage, a route-only degradation never selects sidecar restart, and forced failback is not reported successful before current root and route diagnostics converge. Acceptance requires the original trigger, recovery amplification, and browser-visible duration to be attributed separately from correlated node, sidecar, browser, Root, process/I/O, event-loop, SQLite, and skill-handler evidence; absence of a signal must be stated explicitly rather than inferred as health. This review remains open even when the first final campaign passes. | [Realtime Reliability](architecture/realtime-reliability-roadmap.md) |
| `RT-DIAGNOSTIC-COST-001` / `SKILL-RESOURCE-001` | in progress | An A/B measurement on `.805` found 6.9 MB runtime physical writes during a quiet 45 s baseline versus 60.2 MB while calling the 2 KB supervisor-channel endpoint once per second. The endpoint still built durable member/media inventory even though its watchdog contract only needs live authority. The local fix switches this contract to in-memory link and WebRTC authority and reports `collection.profile=critical_path_live_only`; deploy it and repeat the same A/B process-I/O measurement before acceptance. A second audit found authenticated `/api/supervisor/status` returning about 110 KB and taking 2.6 s because every read repeated runtime HTTP/process/slot diagnostics and embedded 45 KB of persisted state plus a 19 KB attempt. The local read-model change makes this route an event projection, removes recursive persisted state, compacts specialized evidence, and keeps browser fallback on the same projection. A consumer audit confirmed the browser uses compact runtime/thin reliability variants and explicit details refreshes, while CLI/autostart use only PID, slot, URL, runtime state, transition role, and start reason. The client now refuses the legacy approximately 1 MB full reliability fallback on both rooted and loopback recovery paths; the targeted Angular suite passed 24/24. The loopback UI must keep current local WS/YWS authority separate from remote root-relay quality while retaining relay instability in diagnostics. Before closure, repeat latency/size and process-I/O A/B measurements locally and on `.30`, verify snapshot age stays bounded during healthy monitoring, and prove repeated status/public-status reads do not change NATS, process, file-write, or skill counters. The same process audit found `slideshow_skill` holding dozens of SQLite/WAL descriptors and writing 9.5 MB during the six-minute soak because Python's SQLite context manager committed but did not close. Direct stand release `0.1.66` assigns polling to one service-process loop, reuses one locked read-only SQLite connection, suppresses expected health-probe disconnect traces, and declares explicit I/O, descriptor/handle, and thread budgets; these stand-only changes are now incorporated in the versioned `0.1.67` registry candidate instead of remaining deployment drift. A direct `.30` deployment kept the active slideshow advancing with eight stable descriptors and no log growth; over 30 s physical writes fell from the old approximately 91.6 KB/s baseline to 17.5 KB/s (about 81%) and write syscalls from approximately 24.8/s to 4.3/s (about 83%). Core now samples the service process tree off the event loop, exposes cached resource activity through status, and emits a rate-limited issue when a declared sustained budget is exceeded without automatically masking the defect by killing the skill. After core rollout, run a longer local and `.30` soak, prove snapshot age and process identity stay bounded, exercise folder/index/favorite operations, and confirm the issue stream attributes an injected budget violation to the responsible skill. | [Realtime Reliability](architecture/realtime-reliability-roadmap.md), [Runtime Guarding](architecture/runtime-guarding.md) |
| `RT-EVENT-LOOP-BOUNDARY-001` | in progress | After the `.30` `0.1.863` cutover, watchdog evidence attributed a 1.462 s stall to synchronous `cryptography.x509.sign` inside `RTCPeerConnection` construction and a 580 ms stall to synchronous `sqlite_store.execute` from the personalization header endpoint; both had `skill_candidates=[]`, while skill circuits and handler drift remained zero. WebRTC peer construction now runs in the bounded default executor and synchronous personalization routes run in FastAPI workers. A direct-patch restart then stayed stall-free for 19 minutes, but an 80-header/200-ping contention test exposed two more core boundaries: 669 ms in `service_supervisor._watchdog_loop -> Popen.poll` and 662 ms while sending an HTTP response. The header path was parsing and rewriting a 2.4 MB personalization state file on every successful self-read because 2000 audit records shared the 6.7 KB operational state. The local follow-up batch makes service status an event/background-probe projection, batches child polling off-loop, suppresses durable allow-audit for frequent self-reads while retaining denials and mutations, and adds a bounded single-flight header cache with hit/miss/wait/compute/invalidation/source-size diagnostics. Direct `.30` validation completed 80 header requests at p95 66.170 ms and 200 concurrent pings at p95 28.702 ms with one cache computation, no personalization file growth, and zero watchdog stalls. A later independent interruption was preceded by a Root HTTP connect timeout and about 39.5 seconds of remote receive silence while five NATS PINGs remained unresolved; no event-loop stall, skill attribution, or large download overlapped the trigger. Recovery completed in about 0.82 seconds, but the reconnect incorrectly consumed a sidecar overlap slot and was labeled handoff. The follow-up therefore also makes unresolved PING age authoritative readiness evidence, serializes local-client admission, and gates parallel relays on verified supervisor warm-switch PID/instance ownership. Repeat the exact contention and reconnect tests locally and on `.30`; acceptance requires one computation per cache-miss key, no successful-read persistence growth, all header and ping responses successful, bounded ping p95/max, zero new watchdog stalls, no skill attribution/circuit, and ordinary reconnect counters showing cleanup without admitted overlap. Reconnect a real browser to execute fresh DTLS certificate generation and prove no `x509`, personalization, SQLite, process-poll, response-send, or sidecar-admission watchdog frame remains. Preserve this item for the independent `RT-POST-INCIDENT-REVIEW-002` log review even after the campaign passes. | [Runtime Guarding](architecture/runtime-guarding.md), [Realtime Reliability](architecture/realtime-reliability-roadmap.md) |
| `RT-SIDECAR-SESSION-OWNERSHIP-001` | in progress | A 2026-08-18 `.30` incident proved that `active_session=true` plus a historical remote-connect timestamp could remain superficially ready during about 39.5 seconds of remote silence and five unresolved runtime NATS PINGs. The subsequent ordinary reconnect was admitted as an A/B overlap even though no warm switch existed. The fix makes unresolved PING age authoritative, resolves loopback owner PID/runtime instance off-loop, serializes admission, permits overlap only for a verified ready supervisor candidate, and replaces a stale relay sequentially for the same owner. Local managed-sidecar restart preserved `500/500` runtime pings at p95 68.085 ms without changing the runtime PID. On `.30`, an exact loopback NATS socket cut preserved `500/500` pings at p95 5.148 ms, opened one replacement Root session in about 1.7 seconds, and left overlap/reject counters at zero. A later 25-second remote DROP correctly crossed `remote_unresponsive` after 6.191 seconds while preserving `513/513` local pings, but exposed a diagnostic parser defect: exact-payload PONG matching left three false outstanding PINGs after traffic resumed. The follow-up parser is streaming and payload-aware across coalesced/fragmented `MSG/HMSG/PUB/HPUB` frames. The prolonged post-test ingress outage exposed a second recovery amplifier: runtime handshake timeouts replaced the local client and reset sidecar retry delay to one second, reaching 235 failed handshakes and 94 same-owner replacements. Remote attempts now share one process-wide jittered backoff that survives local session generations and resets only after a stable relay. Direct `.30` evidence preserved all sampled pings while the streak advanced across four replacement sessions to the 15-second ceiling instead of restarting. Two post-fix 22-second DROP campaigns after Root recovery preserved `258/258` and `207/207` runtime pings at p95 1.829 ms and 1.836 ms. The first kept the existing sidecar session while client PING outstanding peaked at two; the second performed exactly one sequential local/remote reconnect after restore. Both finished with one active session, zero outstanding PINGs, zero failure streak, zero admitted/rejected overlap, and no residual firewall rule. The transport acceptance evidence is therefore complete; the issue stays open for the later independent log/browser review and absolute trigger attribution. The earlier genuine `37.143.14.10:443` outage affected both Windows and `.30`; because both share a NAT and the old retry storm may have contributed to ingress blocking, external trigger versus client amplification remains explicitly unresolved. | [Realtime Sidecar](architecture/adaos-realtime-sidecar.md), [Realtime Reliability](architecture/realtime-reliability-roadmap.md) |
| `SKILL-REMOTE-POLL-001` | ready for browser review | The `.30` outage audit found `slideshow_skill` opening Root connections while the sidecar, supervisor, and runtime were also recovering. Its 2.5-second worker called `sdk.redevice.list_endpoints(sync_registry=True)`; the SDK flag controls local registry writes but never disabled the public Root GET. Persisted playback also created poll workers in both the lifecycle runtime and service process before the direct `0.1.66` ownership patch. Core now exposes `list_cached_endpoints()` with a tested no-network contract and `fetch_endpoint_snapshot()` with explicit remote outcome. Registry candidate `0.1.67` uses a process-local projection for every routine tick, bootstraps the disk-backed cache at most once, refreshes Root on a separate 60-second schedule with jittered exponential failure backoff, reuses the validated snapshot when resolving playback commands, applies per-target command backoff, retains one service-process owner, and exposes process-local cached/remote/command counters and next-eligibility evidence in health. The first `.30` direct check caught and removed two deeper remote lookups per playback tick; after that patch, 31 cached reads advanced the slideshow while remote reads stayed at zero, but an empty local registry also proved that cache-only conversion without a bounded refresh would silently break ReDevice delivery. The final 160-second soak kept one PID and eight descriptors, advanced the active slideshow, completed `630/630` runtime pings at p95 2.036 ms/max 9.150 ms, and recorded 64 cache-only ticks, three scheduled refreshes, 21 successful commands, and zero failures. In the repeated 22-second DROP, the effective 10-second command cooldown reduced failures to one and suppressed the next periodic command; recovery cleared command error/next-retry state and resumed successful delivery. A follow-up after removing per-tick disk reads completed Windows `132/132` pings without a new watchdog/SQLite warning and `.30` `139/139` pings at p95 1.950 ms/max 4.042 ms; neither window performed a remote refresh or another disk bootstrap, while `.30` delivered five commands and advanced the slideshow. The LLM skill guide now requires cache-only interval contracts, one lifecycle owner, explicit refresh outcome/backoff, process-local snapshots for disk-backed registries, projection reuse for periodic commands, and a network-opener failure test. Browser review must confirm the physical ReDevice surface follows the recovered command stream and the slideshow modal remains populated; Root trigger versus retry amplification remains a separate attribution question, and socket coincidence alone is not evidence that this skill caused the original outage. | [LLM Skill Development](guides/llm-skill-development.md), [Runtime Guarding](architecture/runtime-guarding.md) |
| `PERSONALIZATION-AUDIT-STORAGE-001` | open | The channel fix stops high-rate successful self-reads from rewriting the legacy personalization state, but the existing file on `.30` still contains 2000 audit rows: 2.4 MB total versus about 6.7 KB without `audit`. Move audit records to a separately bounded append-oriented or transactional store instead of relying on the header cache to hide monolithic write cost. Migrate legacy records without losing denials or mutations, define retention and compaction, expose migration/error/size/write-amplification diagnostics, and prove concurrent policy evaluation remains consistent. Acceptance requires operational personalization mutations to avoid rewriting audit history and to remain bounded under the same header/ping/watchdog contention campaign. | [Runtime Guarding](architecture/runtime-guarding.md) |
| `RT-MEMORY-ATTRIBUTION-001` | in progress | On 2026-08-17 the local supervisor restarted a healthy runtime twice during the client Angular production build. The captured pre-restart evidence showed only about 125 MB in the complete runtime family while system available memory fell to about 812 MB; the old policy acted solely on the host-wide 5% threshold, so restarting AdaOS did not remove the external `node` allocation and unnecessarily interrupted the channel. The local fix classifies sustained pressure as `runtime_family` only when fresh family RSS or baseline growth crosses a host-scaled anomaly threshold; otherwise it records `external_or_system`, preserves runtime, and writes a cross-platform evidence snapshot containing the largest host processes, cumulative process I/O, runtime-family membership, and skill-runtime identity. A local controlled external allocation reduced free memory to 499 MB for longer than the 20 s guard window: evidence attributed 4.24 GB to the allocator, measured the runtime at 160 MB, kept the same runtime instance and supervisor PID, and left root control and route `ready`. The audit also found that Windows service-skill launchers may exit after creating a verified listener; the generic stop path now revalidates listener ownership and terminates that owned tree, allowing critical skill RSS to select a bounded skill quarantine through the runtime lifecycle API instead of restarting the channel. Acceptance still requires the equivalent `.30` campaign and a synthetic service-skill pressure test proving the responsible skill is stopped, enters cooloff, and is named in both its issue stream and supervisor evidence. Re-run the later `RT-POST-INCIDENT-REVIEW-002` analysis against these snapshots and compare pre-degradation process/I/O windows with quiet baselines. | [Runtime Guarding](architecture/runtime-guarding.md), [Realtime Reliability](architecture/realtime-reliability-roadmap.md) |
| `CORE-UPDATE-IO-001` | ready for stand | `.805` preparation copied the 350 MB, 12,471-file seed venv for 404.711 s on non-reflink ext4 and then spent 109.495 s in `uv sync`; the locked local `y-py` source was rebuilt for about 105 s even though the seed already contained the matching package. The `.858` rollout reproduced the mechanism independently: GNU `cp -a --reflink=auto` silently became a full 309 MB copy on ext4, remained in `folio_wait_bit_common` for more than 350 s, drove 46-91% iowait with 6-10 blocked processes, and coincided with 6-10.5 s SQLite writer-gate waits. An isolated fresh locked `uv sync` completed in 48.771 s but still wrote 335.1 MB; selecting `hardlink` only when the immutable uv cache and candidate target share a filesystem completed offline in 0.509 s, wrote 2.6 MB, and kept all runtime probes successful. The local implementation now skips the eager active-venv copy, records the active environment as a deferred offline fallback, installs from the version-locked uv cache with background I/O priority, and falls back to a private full copy only after locked uv failure. Deploy it through one complete A/B update and preserve preparation duration, link-mode decision, physical I/O, blocked-task/iowait, runtime/sidecar/browser-route latency, SQLite waits, and skill process continuity before closure. | [Realtime Reliability](architecture/realtime-reliability-roadmap.md) |
| `CORE-UPDATE-STATE-002` | in progress | A Windows supervisor campaign reproduced an automatic A-to-B rollback roughly three minutes after slot A had reached `validated/root_promotion_pending`: generic restart/apply timeout reconciliation treated the operator-owned promotion wait as a failed transition. A second campaign also proved that a SHA-pinned request could be silently replaced by the current remote branch head before cutover. Keep the validated slot active until explicit complete or rollback, preserve immutable SHA pins fail-closed, expose `prepare` and `prewarm` as distinct live phases without a runtime status round-trip, and distinguish Windows task `Status: Running` from `Scheduled Task State: Enabled` so slot B is reported at its live `8778` endpoint. Repeat the pinned cutover, wait beyond the configured timeout, and preserve requested/resolved commit, active-slot, sidecar, browser-route, status-latency, process/I/O, and skill-blocking evidence before closure. | [Realtime Reliability](architecture/realtime-reliability-roadmap.md) |
| `SKILL-RUNTIME-ISOLATION-002` | in progress | The Windows cutover campaign observed `slideshow_skill` running `pip install --upgrade pillow` and `pytest` while core slot preparation was active; runtime readiness then disappeared in repeated windows even though upstream state remained cached as ready. The migration guard was process-local, so retiring and promoted core slots could start independent migrations and overwrite one shared status. A later final deployment reproduced the lifecycle race independently: `.30` returned `database is locked` from a local install during core transition, then the API client timed out after 30 seconds while the server-side operation continued and promoted the new slot; the service process still required explicit activation to converge on that slot. On Windows, the first activation correctly failed closed because the active runtime had cached the pre-deployment SDK ABI even though the slot files already matched, and succeeded after one supervisor-managed runtime restart. Keep one cross-process migration lease, serialize core-update admission against that lease, run migration work outside the API event-loop process at background OS priority, terminate only the owned worker tree with runtime shutdown, and expose redacted PID/command/CPU/RSS/read/write diagnostics for active `pip`, `pytest`, sync, and service children. A timed-out client must retain an operation ID and observe the eventual result instead of reporting a terminal failure while work continues; slot activation must atomically restart or reload the service from the selected slot, and SDK ABI drift must be detected before prepare. Prove on Windows and `.30` that simultaneous migration/update requests produce a retryable admission result, no duplicate worker, no orphan child, bounded channel latency, an attributable blocker record during a deliberately heavy skill dependency/test workload, and exact agreement among operation, active-slot, loaded-handler, and service-process status. | [Runtime Guarding](architecture/runtime-guarding.md), [AdaOS Supervisor](architecture/adaos-supervisor.md) |
| `UI-RT-001` | in progress | Re-run one shared channel/materialization matrix on Windows and `.30`: Infrastructure State skill/scenario lists, Subnet Environment snapshot, Weather city mutation, Marketplace homepoint install of the AdaOS Drive skill/scenario, Mediacenter and missing mediaserver desktop navigation, slideshow index modal, and Research Workbench project artifacts/consensus/development sections. Preserve browser action correlation, node-side skill-log tail, active route, lifecycle scope, YDoc teardown reason, and backend tool result for every failure. A backend method succeeding while the widget reports `Data is temporarily unavailable` is a channel/materialization defect, not closure of that symptom. | [UI Runtime Diagnostics](architecture/ui-runtime-diagnostics.md) |
| `UI-CATALOG-AUTHORITY-001` | in progress | The `91.98.89.76` Infrascope/home reproduction kept Root, WS, WebRTC, YWS, and state sync ready, but returning home rebuilt `web_desktop` with `catalog.widgets=[]`. Persisted Yjs had hidden that every workspace skill directory was a stale remnant without `skill.yaml`, while executable handlers and `webui.json` lived in selected immutable runtime slots. Production catalog loading now follows the same active version/slot authority as handler execution and reports `source_authority`, version, slot, and resolved manifest; Infrascope snapshot requests move to a bounded/coalesced executor because synchronous EventBus adaptation has no running loop in the handler thread. Acceptance requires local and stand switch loops with workspace sources absent, a nonempty stable catalog after every return home, no transport relabeling caused by widget readiness, bounded snapshot queue/latency, no new event-loop stall, and exact agreement between declaration and loaded-handler slots. After the fault/soak run, perform a separate later log review for catalog loss, stale Yjs masking, handler/executor starvation, skill circuit activity, process/download I/O, route churn, and contradictory browser/node/Root evidence before closure. | [LLM Skill Development](guides/llm-skill-development.md), [UI Runtime Diagnostics](architecture/ui-runtime-diagnostics.md), [Rebuild Lag Hardening](architecture/realtime-rebuild-lag-hardening.md) |
| `RUNTIME-COMPAT-HOTPATH-001` | ready for stand | A real Infrascope stream request on `91.98.89.76` fanned out to 15 skill subscriptions; receiver prefilters synchronously attempted development-ticket and pending-action publication for normal foreign receivers, called blocking YDoc access from the active event loop, delayed the event API beyond 3 s, and produced route starvation. Runtime prefilters now perform admission and sampled logging only; compatibility-ticket creation remains an explicit validate/install/migrate operation. Repeat the stream burst with an active browser and prove bounded publish latency, zero compatibility writes/warnings on the EventBus path, completed bounded Infrascope workers, no route starvation, and stable supervisor/runtime probes. | [LLM Skill Development](guides/llm-skill-development.md), [Runtime Guarding](architecture/runtime-guarding.md) |
| `SKILL-MODULE-AUTHORITY-001` | in progress | Infrascope workers completed in the subscription-loaded `handlers/main.py`, while diagnostics and lifecycle tools imported a second synthetic copy and reported zero counters; the same split allowed `drain`/`dispose` to return success without touching the executor that owned event work. Tool execution now reuses the fully imported module registered for the exact unchanged active-slot source, with isolated import retained only when no active module exists or source drift is detected. Cross-process diagnostics persist a compact, rate-bounded snapshot with source, age, writer PID and failed-write evidence. Acceptance requires module-identity and source-drift tests, a real lifecycle drain against active work, slot migration and scenario-switch soak, nonzero worker diagnostics through `/api/tools/call`, zero stale publish after drain, and no duplicate subscription declarations. | [LLM Skill Development](guides/llm-skill-development.md), [Runtime Guarding](architecture/runtime-guarding.md) |
| `MEDIA-CENTER-001` | ready for browser review | The scenario now passes scalar filter parameters instead of an unevaluated action-expression object, provides exact video/audio/favorites predicates, keeps modal and table favorite state consistent, binds initial search focus, makes table rows keyboard-selectable, fixes the detached media-player `trackBy`, and deletes a configured folder's exact catalog/core references under a cross-process lease without deleting source files. Local Playwright acceptance proved initial focus, `All -> Recent -> Favorites -> Clear search -> first row` Tab order, Enter playback, no renderer fallback, a non-empty media URL, an actual advancing MP4, and favorite toggle/restore propagation to the table. Local and `.30` tool/API acceptance proved exact media kinds (`.30`: 176 video and zero audio), `206` byte ranges, favorite round trips, source-preserving folder cleanup, and retryable concurrent-delete rejection; during the held lease `.30` served 24/24 pings at 1.63 ms average and 3.07 ms maximum. Complete one final visual playback/folder-removal pass in the already trusted `.30` browser; a new headless device reaches the node HTTP/control surface but is correctly stopped at device authorization before Yjs hydration. | [LLM Skill Development](guides/llm-skill-development.md), [UI Runtime Diagnostics](architecture/ui-runtime-diagnostics.md) |
| `MEDIA-RELAY-RESILIENCE-001` | in progress | The 2026-08-19 local incident was not a Root, NATS, memory-pressure, or skill-process failure. A second 8.80 GB MKV on `\\server\home\Video` held synchronous SMB `read()` calls on the runtime event loop for 26.5 s and 73.4 s. During the recoverable intervals the node published about 1.07 GB without consumer credit; nginx then preempted the backend's 120 s cleanup boundary at 60 s, Root sent `media_http_abort` only after the browser closed, and the blocked runtime could not process it before supervisor self-heal. The coordinated fix isolates media resolution/stat/open/seek/read/close in a dedicated bounded executor, caps active relays, adds `media_ack_v1` credit from Root HTTP `write/drain`, explicitly detaches browser media sources, aligns nginx outside the backend timeout, and records slow-I/O source kind/path digest, active workers, in-flight chunks, ACKs, aborts, and busy rejects without logging source paths. A tracked local overlay then repeated `Hamnet.2025.1080p.WEB-DL.DDP5.1.H.264.mkv`: a 1 MiB routed range produced no bytes before the 90.05 s client deadline, but the runtime kept one PID and served 167/167 pings at p95 58.8 ms/max 282.6 ms; Root recorded zero relayed chunks and a prompt `client_closed` abort. Direct PowerShell/.NET SMB access completed stat/open/1 MiB read in 23/13/161 ms, while independent Python `Path.resolve(target)` and `os.stat` each exceeded a 19 s deadline and Python open/read later completed in 2508/22 ms. Operation diagnostics then isolated a 9.391 s `resolve_reference` followed by a still-active 35.516 s `read`; a later unbuffered 64 KiB read also remained active beyond 46.922 s, proving that chunk size and `BufferedReader` were not the root cause. Windows UNC reads now cross an async child-process boundary with private binary IPC, open/read deadlines, one bounded first-read retry and immediate browser-abort termination, while local/Linux reads keep the executor path. On the same Hamnet file, direct child open/read completed in 65.8/1.2 ms; two routed requests then returned `206` for 64 KiB in 392.6 ms and a separate seeked 1 MiB in 242.6 ms with one runtime PID, all pings successful, seven chunks, no slow operation, timeout, retry, abort or orphan reader. A deterministic stalled-child test also proves read deadline and process cleanup. Runtime status preserves active, total, slow, last and maximum duration by operation and exposes reader processes/starts/timeouts/retries in `adaos node reliability`. The `.30` Linux regression passed all nine route/reader tests from the same source and then exercised the deployed protocol through the real `ru.api.inimatic.com` media relay: a 1 MiB range returned `206` in 127.7 ms, an intentionally closed 100 MiB request produced one observed abort and zero residual tasks/readers, and a seeked 1 MiB follow-up returned `206` in 123.1 ms. The runtime kept PID `550822`; media I/O maxima stayed below 3 ms, route publish/drop errors stayed zero, and the local server remained available throughout. Complete a real browser Hamnet playback/close/open-second pass and the broader Windows cancellation/concurrency soak before closure. Compare node, supervisor, sidecar, browser, Root backend, nginx, process-I/O, skill-budget, and storage evidence against a quiet baseline; explicitly confirm no skill handler or unrelated download overlaps the trigger. After the fault/soak campaign, perform one independent repeat log analysis before closure even when every functional check passes. | [Realtime Reliability](architecture/realtime-reliability-roadmap.md), [LLM Skill Development](guides/llm-skill-development.md) |
| `MEDIA-CODEC-COMPAT-001` | open | Local analysis of `Spider-Man.3.2007.bdrip_[2.18]_[teko].avi` separated storage and codec failures. The catalog reference used `\\server\home`, while the same WinFsp/rclone mount read reliably through `A:`; mapped-drive aliasing reduced real reference resolution plus first 64 KiB read to about 100 ms instead of a 12 s zero-byte relay timeout. The AVI header identifies Xvid video, which Chromium does not decode, so a generic "missing source; re-scan" message is false. Keep direct range playback for compatible resources, add bounded container/video/audio probing and an isolated media-skill rendition worker for H.264/AAC browser output, and register completed renditions through the core media boundary. Admission must limit concurrent workers, CPU, RSS, I/O and cache bytes; cancellation, source revision changes and disk exhaustion must be observable and leave no child process or partial advertised rendition. Acceptance requires the real Spider-Man item to produce an explicit `rendition_required -> rendition_ready` plan, play and seek in the browser, survive close/open-second, and keep runtime, NATS, command, sync and status probes within the same reliability budget on Windows and `.30`. | [Media Center roadmap MC7-03/04](architecture/media-center-roadmap.md), [Media Center MVP](architecture/media-center-mvp.md), [LLM Skill Development](guides/llm-skill-development.md) |
| `MEDIA-MAPPED-IO-001` | in progress | Windows WinFsp/rclone evidence shows that both `A:` and its `\\server\home` alias can alternate between sub-100 ms reads and a bounded read timeout. Mapped drive letters are now classified as `mapped_drive` and use the same killable relay-reader, deadline, one first-read retry and source-kind/path-digest diagnostics as UNC. Local 32 MiB direct delivery sustained 11.19 MiB/s while 32/32 runtime pings passed (p95 73 ms, max 240 ms); a later isolated attempt timed out at 8 s without taking down runtime, followed by four successful mapped/UNC reads. Linux `.30` isolated verification compiled all changed modules, resolved the synthetic mapping inside its root, classified the drive as network storage and read `RIFF`. Closure requires a real routed browser Spider-Man/Hamnet close/open-second soak on Windows and `.30`, confirmation that timeout/retry/orphan counters settle, and an independent post-run log review. | [Realtime Reliability](architecture/realtime-reliability-roadmap.md), [LLM Skill Development](guides/llm-skill-development.md) |
| `NER-001` | in progress | Complete the canonical read model across device, node, workspace, scenario, skill, and manifest sources. | [Named Entities](architecture/named-entities.md), [NLU Roadmap](architecture/nlu-roadmap.md) |
| `NER-005` | in progress | Finish authoritative lifecycle/conflict events and use `entity.registry.changed` as the shared invalidation signal. | [Operational Event Model](architecture/operational-event-model-roadmap.md) |
| `NER-006` | open | Migrate remaining client/operator consumers to canonical refs and delete duplicate display-name fallbacks after coverage exists. | [Device Access Roadmap](architecture/device-access-roadmap.md) |
| `BUILDER-LEGACY` | open | Move the remaining Prompt IDE DEV file lifecycle behind `adaos.sdk.developer.projects` before legacy retirement. | [Builder SDK Boundary](architecture/builder-sdk-boundary.md) |
| `BUILDER-RELOAD` | in progress | Preserve widget and coarse branch identity on no-op semantic reloads and pass reconnect/reload soak coverage. | [Builder Roadmap](architecture/builder-roadmap.md#phase-8-product-experience), [Webspace Evolution](architecture/webspace-evolution-roadmap.md) |
| `BUILDER-CHANNEL-ACCEPTANCE` | ready for human stand | Complete one human wide/compact Web pass and one physical Telegram-button click with backend receipt. Addressed “что выбрано?”, canonical project topic, five bounded controls, token admission, cross-process DEV dispatch, and originating-conversation reply are locally proven; mutating controls without a registered executor are intentionally withheld and hard channel parity remains out of scope. | [Builder Roadmap](architecture/builder-roadmap.md#phase-11-conversational-development-control-plane), [Builder Conversational Development](architecture/builder-conversational-development.md#channel-capability-boundary) |
| `AP7-14` | in progress | Merge infra `5f9a5b0`, then prove one clean production deploy with candidate warming outside `inimatic_proxy`, admission only after health, strict continuous public samples, and no automatic repeat of the already committed rollout. | [Artifact Pipeline Roadmap](architecture/artifact-source-package-activation-roadmap.md#milestone-ap7-end-to-end-proof-and-legacy-retirement-decision) |
| `DISTRIBUTED-RUNTIME-001` | in progress | Implement the AP8 ProjectDeployment boundary and DS1-DS4 service/data topology in dependency order, prove SDK-only Media Center and non-media fixtures locally, then execute the reviewed two-node deployment, fencing, partial-route, drain and interruption campaign. | [Artifact Pipeline AP8](architecture/artifact-source-package-activation-roadmap.md#milestone-ap8-distributed-project-deployment), [Distributed Service/Data Roadmap](architecture/distributed-service-and-data-topology-roadmap.md) |
| `MEDIA-CENTER-DISTRIBUTED-001` | in progress | Deliver all non-deferred MC1-MC7 slices over the generic deployment/topology SDK: agents, resumable indexing, catalog/search/collections, persistent playback/control, TV/desktop/mobile UI, personalization/voice, enrichment/renditions, resource hardening and exact local/`.30` evidence. | [Media Center Roadmap](architecture/media-center-roadmap.md) |
| `PROJECT-DEPLOYMENT-ATOMIC-001` | open | Make deployment configure define-plus-plan atomic, or compensate the desired revision when verified release/package admission or planning fails. The `.30` `0.6.50` campaign truthfully CAS-rejected stale retries but persisted revisions `52` and `53` before their plans failed on an empty verified release cache. | [Media Center stand receipt](architecture/distributed-media-center-stand-validation-2026-08-21.md) |
| `PROJECT-DEPLOYMENT-AUTHORITY-001` | validated-local; stand review pending | Core commit `2b27ca5b` introduces one loopback-only active-runtime deployment authority and routes every `adaos.sdk.deployment` read and mutation, including bounded node inventory, through it. CLI, skill and candidate-runtime processes are clients; an unavailable or mismatched authority fails explicitly, candidate promotion switches ownership, and slow apply remains bounded at ten minutes. Local API/SDK/CLI, candidate lifecycle, inventory-equality and fail-closed suites pass. The original `.30` `0.6.53` process-local inventory failure remains the stand regression: verify the exact rollout preserves its live member and converges service activation before closure. | [Project Deployment SDK](sdk/deployment.md), [Media Center stand receipt](architecture/distributed-media-center-stand-validation-2026-08-21.md), [Distributed Service/Data Roadmap](architecture/distributed-service-and-data-topology-roadmap.md) |

## Could

| ID | State | Work | Owner |
| --- | --- | --- | --- |
| `LRLT-003` | open | Add media-indexer timing, bounded concurrency/payloads, and a focused stall reproducer if media load becomes an active bottleneck. | [Realtime Reliability](architecture/realtime-reliability-roadmap.md) |
| `MRI-004` | open | Document weather provider selection and API-key behavior so provider failures are distinguishable from modal/rendering failures. | [Post-Deploy E2E](architecture/post-deploy-e2e-testing.md) |
| `BUILDER-EVIDENCE-ACTIONS` | open | Add governed open/copy actions for Automation evidence files. | [Builder Roadmap](architecture/builder-roadmap.md#phase-8-product-experience) |
| `RESEARCH-WORKBENCH-READ-001` | ready for stand | Local `desktop-dev` proved all project-detail read surfaces after existing but uninitialized research Projects were changed from HTTP failures to typed empty states. Deploy the canonical `research_orchestrator_skill` release, then repeat `get_direction`, artifacts, consensus, and AutomationBrief reads for initialized and uninitialized Projects on a clean stand; verify the browser shows empty guidance rather than `Data is temporarily unavailable`, and verify an unknown Project still fails closed. | [Research Fabric](architecture/research-fabric.md), [Post-Deploy E2E](architecture/post-deploy-e2e-testing.md) |
| `UILOG-LLM-GROUPING` | open | Add richer LLM-oriented grouping beyond the bounded diagnostic aggregation required by `UILOG-001`. | [UI Runtime Diagnostics](architecture/ui-runtime-diagnostics.md) |

## Deferred

| ID | Resume condition | Owner |
| --- | --- | --- |
| `DIRECT-YJS-DENY-BY-DEFAULT` | Resume after M1 warnings and M3 migrations provide a measured exception inventory. | [Semantic State Plane](architecture/semantic-state-plane.md) |
| `CROSS-SKILL-PROJECTION-CLEANUP` | Resume after the three M3 pilot skills pass stand acceptance. | [Projection Subscription Roadmap](architecture/projection-subscription-roadmap.md) |
| `SIDECAR-YJS-AUTHORITY` | Resume after current transport handoff and MVP SyncChannel recovery are accepted. | [Realtime Reliability](architecture/realtime-reliability-roadmap.md) |
| `REMOTE-MCP-HUB-BRIDGE` | Resume only when public remote Root MCP access to local hubs becomes a product requirement. | [Root MCP Roadmap](architecture/root-mcp-roadmap.md) |
| `BUILDER-AUTONOMOUS-REPRODUCTION` | Resume only after the non-Builder pipeline has also passed human Web/Telegram acceptance and a separate characterization plan prevents self-hosting regressions. | [Builder Roadmap](architecture/builder-roadmap.md) |
| `BUILDER-MODULE-DECOMPOSITION` | Resume under a separate characterization-test plan after MVP behavior is stable. | [Builder Roadmap](architecture/builder-roadmap.md) |
| `UILOG-LLM-DEBUG-WORKFLOW` | Resume when the governed Builder repair loop consumes skill-scoped diagnostics. | [Governed Evolution Roadmap](architecture/governed-evolution-roadmap.md) |

## Evidence Rules

- Link local, stand, canary, and production evidence without promoting one
  maturity level to another.
- Store verbose logs, metrics, browser captures, and dated incident narratives
  in release-evidence artifacts, not in this file.
- Keep only the next falsifiable proof in each active row. Durable contracts
  and full acceptance criteria belong to the owning roadmap.
- Remove a row after closure; the owner roadmap, evidence artifact, commit, and
  Git history retain the completion record.
