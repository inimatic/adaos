Исправь текущую реализацию списка чтения после проверки фиксации. Новая реализация
и ее тесты уже сохранены; не начинай заново и не меняй согласованную компоновку.

Полная проверка skill.yaml обнаружила ошибку в двух data_routes: max_request_hz
и preserve_last_value записаны на уровне маршрута, но по переданному контракту
должны быть внутри read_policy. Исправь вложенность и добавь регрессионную проверку
манифеста, чтобы этот дефект не возвращался. Не ослабляй схему и проверки.

Сохрани работающие CRUD, исправленный поиск по кириллице и текущие формы.
Постоянное хранилище по-прежнему начинает с пустого списка. Полное исходное задание
автоматизации остается в утвержденном brief. Это точечное исправление, не новый
прототип. Не запускай Trial и публикацию; проверки выполняет worker.
