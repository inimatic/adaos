{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "adaos.builder.prototype_acceptance.v1",
  "title": "AdaOS Builder Prototype Acceptance",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema",
    "acceptance_id",
    "project_ref",
    "change_id",
    "revision",
    "webui_digest",
    "request_digest",
    "reviewer",
    "decision",
    "deterministic_evaluation",
    "prototype_resources",
    "behavior_checks",
    "visual_checks",
    "accepted_at",
    "digest"
  ],
  "properties": {
    "schema": { "const": "adaos.builder.prototype_acceptance.v1" },
    "acceptance_id": { "type": "string", "minLength": 1, "maxLength": 160 },
    "project_ref": {
      "type": "string",
      "pattern": "^(project|scenario|skill):[A-Za-z0-9_.-]+$"
    },
    "change_id": { "type": "string", "minLength": 1, "maxLength": 160 },
    "revision": { "type": "string", "minLength": 1, "maxLength": 80 },
    "webui_digest": { "type": "string", "pattern": "^sha256:[a-f0-9]{64}$" },
    "request_digest": { "type": "string", "pattern": "^sha256:[a-f0-9]{64}$" },
    "reviewer": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "kind"],
      "properties": {
        "id": { "type": "string", "minLength": 1, "maxLength": 200 },
        "kind": { "enum": ["human", "agent"] },
        "delegated_by": { "type": ["string", "null"], "maxLength": 200 }
      }
    },
    "decision": { "const": "accepted" },
    "acceptance_stage": { "const": "prototype" },
    "automation_requirements": {
      "type": "array", "maxItems": 32,
      "items": {
        "type": "object",
        "required": ["requirement_ref", "statement", "acceptance", "disclosure", "prototype_refs", "status", "brief_ref", "brief_digest"],
        "properties": {
          "requirement_ref": { "type": "string", "minLength": 1 },
          "statement": { "type": "string", "minLength": 1 },
          "acceptance": { "type": "string", "minLength": 1 },
          "status": { "const": "pending_automation" },
          "prototype_refs": { "type": "array", "minItems": 1, "items": { "type": "string" } },
          "disclosure": { "type": "object", "required": ["en", "ru"] },
          "brief_ref": { "type": "string", "minLength": 1 },
          "brief_digest": { "type": "string", "minLength": 1 }
        }
      }
    },
    "deterministic_evaluation": { "type": "object" },
    "prototype_resources": {
      "type": "array",
      "maxItems": 8,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "resource_type",
          "bundle_digest",
          "definition_digest",
          "generation",
          "record_count",
          "records_digest"
        ],
        "properties": {
          "resource_type": { "type": "string", "pattern": "^prototype\\.[A-Za-z0-9_.-]+$" },
          "bundle_digest": { "type": "string", "pattern": "^sha256:[a-f0-9]{64}$" },
          "definition_digest": { "type": "string", "pattern": "^sha256:[a-f0-9]{64}$" },
          "generation": { "type": "integer", "minimum": 0 },
          "record_count": { "type": "integer", "minimum": 0, "maximum": 1000 },
          "records_digest": { "type": "string", "pattern": "^sha256:[a-f0-9]{64}$" }
        }
      }
    },
    "behavior_checks": {
      "type": "array",
      "minItems": 1,
      "maxItems": 100,
      "items": { "$ref": "#/$defs/check" }
    },
    "visual_checks": {
      "type": "array",
      "minItems": 2,
      "maxItems": 20,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["breakpoint", "viewport", "status", "evidence_ref"],
        "properties": {
          "breakpoint": { "enum": ["compact", "wide"] },
          "viewport": {
            "type": "object",
            "additionalProperties": false,
            "required": ["width", "height"],
            "properties": {
              "width": { "type": "integer", "minimum": 240, "maximum": 10000 },
              "height": { "type": "integer", "minimum": 240, "maximum": 10000 }
            }
          },
          "status": { "const": "passed" },
          "evidence_ref": { "type": "string", "minLength": 1, "maxLength": 500 }
        }
      }
    },
    "accepted_at": { "type": "string", "format": "date-time" },
    "digest": { "type": "string", "pattern": "^sha256:[a-f0-9]{64}$" }
  },
  "$defs": {
    "check": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "status", "evidence_refs"],
      "properties": {
        "id": { "type": "string", "minLength": 1, "maxLength": 160 },
        "status": { "const": "passed" },
        "evidence_refs": {
          "type": "array",
          "minItems": 1,
          "maxItems": 20,
          "uniqueItems": true,
          "items": { "type": "string", "minLength": 1, "maxLength": 500 }
        }
      }
    }
  }
}
