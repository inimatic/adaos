{
  "schema": "adaos.ui.capability_catalog.v1",
  "catalog_version": "1.3.4",
  "webui_schema": "adaos.webui.v1",
  "layouts": [
    {
      "id": "layout.flow",
      "kind": "layout",
      "title": "Linear flow",
      "summary": "One ordered content flow for forms, compact pages, sheets, and modal content.",
      "aliases": {
        "en": ["flow", "stack", "single column", "linear"],
        "ru": ["поток", "стопка", "одна колонка", "последовательно"]
      },
      "manifest": {
        "types": ["single", "stack"],
        "patterns": ["stack", "modal", "sheet", "tabs"]
      },
      "responsive": {
        "desktop": "single ordered track",
        "compact": "single ordered track"
      },
      "postconditions": [
        "Rendered areas retain declaration order.",
        "No area is presented as a parallel peer column."
      ]
    },
    {
      "id": "layout.split",
      "kind": "layout",
      "title": "Primary and supporting regions",
      "summary": "Role-aware main, navigation, detail, auxiliary, and footer placement.",
      "aliases": {
        "en": ["split", "sidebar", "master detail", "side panel"],
        "ru": ["разделенный", "боковая панель", "мастер детали", "панель справа"]
      },
      "manifest": {
        "types": ["split", "responsive"],
        "patterns": ["split", "sidebar-content", "focus-detail", "outline-detail"]
      },
      "responsive": {
        "desktop": "role-aware parallel regions",
        "compact": "main, auxiliary, and footer stack; navigation may use a drawer"
      },
      "postconditions": [
        "At most one area owns each placement role.",
        "Main and supporting regions are parallel on a sufficiently wide viewport."
      ]
    },
    {
      "id": "layout.grid",
      "kind": "layout",
      "title": "Responsive area grid",
      "summary": "An unordered responsive grid of independent page areas; not a workflow board.",
      "aliases": {
        "en": ["grid", "dashboard", "tiles", "overview"],
        "ru": ["сетка", "дашборд", "плитки", "обзор"]
      },
      "manifest": {
        "types": ["grid", "custom"],
        "patterns": ["grid", "dashboard", "desktop-zones"]
      },
      "responsive": {
        "desktop": "auto-fit peer areas with a 280px minimum",
        "compact": "areas wrap into fewer columns"
      },
      "limitations": [
        "Area order has no lane or workflow-state meaning.",
        "Use collection.board for Kanban or status-lane semantics."
      ],
      "postconditions": [
        "Peer areas use the grid render plan instead of the linear flow plan.",
        "Every area remains at least 280px wide before wrapping."
      ]
    }
  ],
  "components": [
    {
      "id": "collection.board",
      "kind": "component",
      "title": "Resource board",
      "summary": "A field-driven board projection of one flat resource collection into ordered lanes.",
      "aliases": {
        "en": ["kanban", "board", "lanes", "task board", "status columns"],
        "ru": ["канбан", "доска", "дорожки", "доска задач", "колонки статусов"]
      },
      "manifest": {
        "widget_type": "collection.board",
        "required_inputs": ["lanes", "laneKey", "titleKey"],
        "optional_inputs": ["descriptionKey", "cardFields", "imageKey", "imageAltKey", "itemIdKey", "minLaneWidth", "showCounts", "compact", "dragDrop", "loadingText", "emptyText", "addItemLabel", "moveItemLabel", "buttons"]
      },
      "data_shape": {
        "kind": "flat_collection",
        "lane_membership": "Each item laneKey value equals one declared lanes[].id.",
        "lane": { "required": ["id", "label"] },
        "item": { "required": ["stable id", "title value", "lane value"], "optional_projection": "cardFields maps arbitrary typed resource fields without domain-specific card code" }
      },
      "events": ["select", "click", "add", "move", "click:<buttonId>"],
      "responsive": {
        "desktop": "all lanes are equal peer columns when width permits",
        "compact": "lane order is preserved in a horizontally scrollable rail"
      },
      "interactions": {
        "query": "Prefer dataSource.kind=resourceQuery for typed filters, search, sort, cursor, and relevance over Resource Workbench.",
        "crud": "Use action.type=resourceOperation for ordinary create/update/delete. action.target is resource_type; params.operation_id selects the declared operation.",
        "button_scope": "inputs.buttons are repeated on every card. Do not put a board-level Add command there; use the lane add event.",
        "drag_drop": "Set inputs.dragDrop=true and declare on=move. The event contains record id, fromLaneId, toLaneId, positions, laneKey, and patch; resourceOperation(update) can persist the patch without a skill-specific method.",
        "accessible_move": "A lane selector is rendered with draggable cards so the same move is available without a dragging gesture.",
        "lane_create": "Declare on=add to expose one add command per lane. The event provides laneId and defaults containing the lane field, but never payload. When required fields must be entered, on=add opens a typed create form; it must not directly create from $event.payload."
      },
      "feature_levels": {
        "implemented": ["ordered lanes", "configurable card fields", "counts", "soft lane limits", "lane create", "drag move", "single-pointer move alternative", "responsive horizontal rail", "optimistic rollback"],
        "composable": ["typed query filters and search", "create/edit form", "record details", "delete/archive commands", "role-gated operations"],
        "deferred": ["swimlanes", "multi-select and bulk move", "lane authoring", "saved personal views", "virtualized very-large boards", "collaborative rank conflict resolution"]
      },
      "limitations": [
        "The renderer never becomes data authority; Resource Workbench or a declared domain command applies mutations.",
        "dragDrop=true requires an on=move persistence action; the renderer only provides optimistic feedback and rollback.",
        "Images are optional and should not be invented unless the request makes imagery relevant."
      ],
      "postconditions": [
        "One rendered lane exists for every declared lane in declaration order.",
        "Every item is rendered in the lane selected by laneKey.",
        "Unknown lane values are reported by validation rather than silently dropped."
      ]
    },
    {
      "id": "ui.list",
      "kind": "component",
      "title": "List and card collection",
      "summary": "Rows, grouped rows, or a responsive card catalog without workflow-lane semantics.",
      "aliases": { "en": ["list", "cards", "catalog"], "ru": ["список", "карточки", "каталог"] },
      "manifest": { "widget_type": "ui.list", "variants": ["list", "cards"] },
      "data_shape": { "kind": "collection" },
      "events": ["select", "click", "add", "click:<buttonId>"],
      "responsive": { "desktop": "rows or auto-fit cards", "compact": "rows or narrower card tracks" },
      "limitations": ["Separate list widgets do not form a semantic Kanban board."],
      "postconditions": ["Every visible collection item has a stable focus identity."]
    },
    {
      "id": "ui.table",
      "kind": "component",
      "title": "Dense table",
      "summary": "A dense comparison of typed textual, status, boolean, icon, and action columns.",
      "aliases": { "en": ["table", "rows", "columns"], "ru": ["таблица", "строки", "столбцы"] },
      "manifest": { "widget_type": "ui.table" },
      "data_shape": { "kind": "collection" },
      "events": ["select", "click:<buttonId>"],
      "responsive": { "desktop": "tabular", "compact": "bounded horizontal overflow" },
      "limitations": ["Image cells and free-form HTML are not supported."],
      "postconditions": ["Declared columns map to rendered cells in declaration order."]
    },
    {
      "id": "ui.form",
      "kind": "component",
      "title": "Typed form",
      "summary": "Typed data capture with validation and bounded form lifecycle actions.",
      "aliases": { "en": ["form", "input", "questionnaire"], "ru": ["форма", "ввод", "анкета"] },
      "manifest": {
        "widget_type": "ui.form",
        "field_type_property": "inputs.fields[*].type",
        "choice_field_types": ["select", "dropdown", "combobox", "singleChoice", "radio"],
        "invalid_field_types": ["selector", "input.selector"]
      },
      "data_shape": { "kind": "record" },
      "events": ["submit", "validate", "save_draft", "reset", "change:<fieldId>"],
      "responsive": { "desktop": "stack or responsiveGrid", "compact": "one column when fields no longer fit" },
      "postconditions": ["Every required answer is represented by a typed field."]
    },
    {
      "id": "item.details",
      "kind": "component",
      "title": "Selected item details",
      "summary": "A readable field projection for one selected or bound record.",
      "aliases": { "en": ["details", "record", "selected item"], "ru": ["детали", "запись", "выбранный элемент"] },
      "manifest": { "widget_type": "item.details" },
      "data_shape": { "kind": "record_or_keyed_records" },
      "events": ["click:<actionId>"],
      "responsive": { "desktop": "detail surface", "compact": "detail surface" },
      "postconditions": ["When selectedStateKey is used, visible details change with that selected key."]
    },
    {
      "id": "collection.tree",
      "kind": "component",
      "title": "Hierarchy tree",
      "summary": "Hierarchical navigation for files, lifecycle nodes, and nested domain records.",
      "aliases": { "en": ["tree", "hierarchy", "files"], "ru": ["дерево", "иерархия", "файлы"] },
      "manifest": { "widget_type": "collection.tree" },
      "data_shape": { "kind": "tree" },
      "events": ["select"],
      "responsive": { "desktop": "nested outline", "compact": "nested outline" },
      "postconditions": ["Every selectable leaf provides all fields consumed by its select action."]
    },
    {
      "id": "ui.actions",
      "kind": "component",
      "title": "Command surface",
      "summary": "Explicit toolbar, segmented, stack, or compact commands.",
      "aliases": { "en": ["actions", "toolbar", "buttons", "segmented", "tabs", "tab bar"], "ru": ["действия", "панель", "кнопки", "переключатель", "вкладки"] },
      "manifest": { "widget_type": "ui.actions" },
      "data_shape": { "kind": "none" },
      "events": ["click", "click:<buttonId>"],
      "responsive": { "desktop": "bounded command group", "compact": "wraps without overlapping" },
      "postconditions": ["Every declared button has an executable or explicitly disabled action contract."]
    },
    {
      "id": "input.toggle",
      "kind": "component",
      "title": "Boolean toggle",
      "summary": "One explicit binary setting with a boolean change event.",
      "aliases": { "en": ["toggle", "switch", "checkbox", "boolean"], "ru": ["переключатель", "флаг", "да или нет"] },
      "manifest": { "widget_type": "input.toggle" },
      "data_shape": { "kind": "boolean" },
      "events": ["change"],
      "responsive": { "desktop": "one bounded setting row", "compact": "one full-width setting row" },
      "interactions": { "event": "The change event is {value:boolean, checked:boolean}; bind page state with $event.checked." },
      "postconditions": ["The visible checked state and emitted value are boolean."]
    },
    {
      "id": "input.selector",
      "kind": "component",
      "title": "Option selector",
      "summary": "One compact selectable option set with optional search.",
      "aliases": { "en": ["select", "dropdown", "option"], "ru": ["выбор", "выпадающий список", "опция"] },
      "manifest": {
        "widget_type": "input.selector",
        "scope": "standalone widget only",
        "form_field_equivalents": ["select", "dropdown", "combobox"],
        "form_field_invalid": "selector"
      },
      "data_shape": { "kind": "options" },
      "events": ["change"],
      "responsive": { "desktop": "anchored popup", "compact": "viewport-bounded popup" },
      "postconditions": ["The selected value maps to one declared option value."]
    },
    {
      "id": "visual.metricChart",
      "kind": "component",
      "title": "Metric chart",
      "summary": "A bounded chart projection for metric or timeseries values.",
      "aliases": { "en": ["chart", "metric", "trend"], "ru": ["график", "метрика", "тренд"] },
      "manifest": { "widget_type": "visual.metricChart", "aliases": ["visual.timeseriesChart"] },
      "data_shape": { "kind": "series" },
      "events": [],
      "responsive": { "desktop": "bounded chart", "compact": "bounded chart" },
      "postconditions": ["A non-empty numeric series produces a non-blank chart."]
    }
  ],
  "recipes": [
    {
      "id": "recipe.kanban_board",
      "title": "Kanban resource board",
      "summary": "Map any typed resource collection into explicit ordered workflow lanes.",
      "aliases": {
        "en": ["kanban", "task board", "board with columns", "planned in progress done"],
        "ru": ["канбан", "доска задач", "доска с колонками", "запланировано в работе готово"]
      },
      "requires": { "layouts": ["layout.flow"], "components": ["collection.board"] },
      "composition": {
        "page_layout": { "type": "single", "pattern": "stack", "areas": [{ "id": "main", "role": "main" }] },
        "widget": {
          "type": "collection.board",
          "area": "main",
          "inputs": {
            "lanes": "Ordered array of {id,label}.",
            "laneKey": "Resource field containing a lane id.",
            "titleKey": "Resource title field.",
            "cardFields": "Optional typed field projections shown on compact cards."
          },
          "dataSource": {
            "prototype_or_crud": { "kind": "resourceQuery", "resourceType": "prototype.<resource_name>", "query": {} },
            "static_preview_only": { "kind": "static", "value": "one flat array of records" },
            "forbidden_kind": "local_crud is a provider mode, never a WebUI dataSource.kind"
          }
        },
        "visual_rule": "Use compact text-first cards. Add images only when the request explicitly makes imagery relevant."
      },
      "interactions": {
        "drag_drop": "Set inputs.dragDrop=true and declare an on=move persistence action. Prefer resourceOperation(update) with record_id=$event.id and payload=$event.patch for ordinary field changes.",
        "crud": "Compose resourceQuery, resourceOperation, ui.form, and item.details before adding skill-specific handlers."
      },
      "postconditions": [
        "The page contains one collection.board widget.",
        "The declared lane count equals the requested column count.",
        "Every requested sample card belongs to exactly one declared lane.",
        "Every lane contains the requested number of sample cards when specified."
      ]
    },
    {
      "id": "recipe.resource_board_workbench",
      "title": "Resource board workbench",
      "summary": "Compose query controls, a board projection, record details, and typed CRUD over one Resource Workbench definition.",
      "aliases": {
        "en": ["editable board", "filterable board", "resource board", "board crud"],
        "ru": ["редактируемая доска", "доска с фильтрами", "доска ресурсов", "crud доска"]
      },
      "requires": { "layouts": ["layout.flow", "layout.split"], "components": ["collection.board", "input.text", "input.selector", "ui.form", "item.details"] },
      "composition": {
        "query": "Toolbar controls write typed search/filter/sort values to page state; resourceQuery consumes those values.",
        "board": "collection.board projects query items by laneKey and writes selection to state.",
        "details": "item.details or a modal reads the selected resource record; ui.form edits it.",
        "operations": "resourceOperation implements create/update/delete and lane-field updates; domain commands remain separate.",
        "prototype": {
          "webui_data_source": { "kind": "resourceQuery", "resourceType": "prototype.<resource_name>", "query": {} },
          "provider_mode": "The separate PrototypeData artifact uses mode=local_crud; never put local_crud in WebUI dataSource.kind.",
          "records": "Return prototype_records beside webui in adaos.builder.webui_result.v1 so Builder can materialize the disposable provider.",
          "forms": "Use ui.form inputs.fields with formInputType values. For a choice field use select, dropdown, or combobox; selector is invalid and input.selector is a separate standalone widget.",
          "query_binding": {
            "control": { "type": "input.text", "actions": [{ "on": "change", "type": "updateState", "params": { "searchQuery": "$event.value" } }] },
            "resource_query": { "search": "$state.searchQuery" }
          },
          "board_create_entry": {
            "on": "add",
            "type": "openModal",
            "params": { "modalId": "create-item" },
            "rule": "The create-item modal contains a ui.form with title and laneKey fields; do not add a repeated per-card Add button."
          },
          "board_edit_entry": [
            { "on": "click:edit", "type": "updateState", "params": { "selectedRecordId": "$event.id", "selectedRecord": "$event" } },
            { "on": "click:edit", "type": "openModal", "params": { "modalId": "edit-item" } }
          ],
          "actions": {
            "create_form_submit": { "on": "submit", "type": "resourceOperation", "target": "prototype.<resource_name>", "params": { "operation_id": "create", "payload": "$event.values" } },
            "edit_form_submit": { "on": "submit", "type": "resourceOperation", "target": "prototype.<resource_name>", "params": { "operation_id": "update", "record_id": "$state.selectedRecordId", "payload": "$event.values" } },
            "move": { "on": "move", "type": "resourceOperation", "target": "prototype.<resource_name>", "params": { "operation_id": "update", "record_id": "$event.id", "payload": "$event.patch" } },
            "delete": { "on": "click:delete", "type": "resourceOperation", "target": "prototype.<resource_name>", "params": { "operation_id": "delete", "record_id": "$event.id" } }
          },
          "modal_rule": "When openModal is used, declare that exact literal id under ui.application.modals and provide a schema object containing widgets. In a complete JSON document use create-item, never @create-item; @<id> is reserved for JSON Patch pointer paths."
        }
      },
      "postconditions": [
        "Changing a query control changes the resource query rather than filtering an unrelated client copy.",
        "Selecting a card identifies one stable resource record for details or editing.",
        "Ordinary CRUD does not require a generated skill-specific handler.",
        "Every mutation is checked against the ResourceDefinition operation and role policy."
      ]
    },
    {
      "id": "recipe.master_detail",
      "title": "Master and detail",
      "summary": "Select one collection item and show a state-bound detail surface.",
      "aliases": { "en": ["master detail", "selected item", "side details"], "ru": ["список и детали", "выбранный элемент", "детали справа"] },
      "requires": { "layouts": ["layout.split"], "components": ["ui.list", "item.details"] },
      "composition": { "selection": "Master writes selected id; details read the same id." },
      "postconditions": ["Selecting different master records changes the visible detail record."]
    },
    {
      "id": "recipe.application_manager",
      "title": "Application lifecycle manager",
      "summary": "A full-screen Extensions-style catalog and installed Application workbench backed by public Root MCP contracts.",
      "aliases": {
        "en": ["applications", "application manager", "extensions marketplace", "installed applications", "mcp applications"],
        "ru": ["applications", "приложения", "управление приложениями", "маркет приложений"]
      },
      "requires": {
        "layouts": ["layout.split"],
        "components": ["input.toggle", "input.selector", "ui.list", "item.details", "input.commandBar", "ui.actions"]
      },
      "composition": {
        "page": "Use a quiet full-surface split layout with pattern=sidebar-content, sidebarWidth=340, the catalog area role=sidebar, and the detail area role=main. The compact renderer exposes the catalog as a drawer.",
        "navigation": "One input.toggle writes installedOnly from $event.checked. One ui.list renders applications.list, uses its built-in search, projects nested Application read-model fields, and writes selectedApplicationId from $event.application.application_id.",
        "catalog_widget": {"type": "ui.list", "inputs": {"variant": "list", "itemIdKey": "application.application_id", "search": true, "searchPlaceholder": "Search applications", "titleKey": "application.display.title", "subtitleKey": "application.publisher.display_name", "previewKey": "application.display.summary", "emptyText": "No applications found."}, "action": {"type": "updateState", "on": "select", "params": {"selectedApplicationId": "$event.application.application_id"}}},
        "installed_filter": {"type": "input.toggle", "inputs": {"label": "Installed only", "defaultValue": false}, "action": {"type": "updateState", "on": "change", "params": {"installedOnly": "$event.checked"}}},
        "details": "A state-only item.details reads applications.show whenever selectedApplicationId is non-empty and binds installationRevision, subscriptionRevision, applicationInstalled, applicationRemovable, and effectiveReleaseDigest from the read model. Installation/subscription/effective-release records may be absent, so their bindings default to zero or an empty digest. An input.commandBar visible only after selection uses inputs.variant=segmented, inputs.selectedStateKey=activeTab, widget-level click action bound to $event.id, and buttons Overview, Versions, Operations, and Reports controls visibleIf content.",
        "details_widget": {"type": "item.details", "visibleIf": "$state.selectedApplicationId", "inputs": {"stateOnly": true, "stateBindings": {"installationRevision": {"path": "installation.revision", "default": 0}, "subscriptionRevision": {"path": "subscription.revision", "default": 0}, "applicationInstalled": "installed", "applicationRemovable": "application.protection.active_installation_removable", "effectiveReleaseDigest": {"path": "effective_release.release_digest", "default": ""}}}},
        "overview_widget": {"type": "item.details", "visibleIf": "$state.activeTab == 'overview' && $state.selectedApplicationId", "inputs": {"fields": [{"label": "Summary", "path": "application.display.summary"}, {"label": "Publisher", "path": "application.publisher.display_name"}, {"label": "Visibility", "path": "application.visibility"}, {"label": "Lifecycle", "path": "application.lifecycle"}, {"label": "Installed", "path": "installed"}, {"label": "Update available", "path": "update_available"}, {"label": "Track", "path": "effective_release.update_track"}, {"label": "Release status", "path": "effective_release.reason"}]}},
        "ordering": "Within the detail area place the tabs first, lifecycle actions and reviewed-plan receipt next, then settings and the active tab content. Lifecycle actions must stay above the potentially scrolling Overview.",
        "tab_action": {"type": "updateState", "on": "click", "params": {"activeTab": "$event.id"}},
        "reads": {
          "catalog": {"kind": "mcp", "toolId": "applications.list", "arguments": {"installed_only": "$state.installedOnly"}, "dryRun": true, "resultPath": "response.result.applications"},
          "application": {"kind": "mcp", "toolId": "applications.show", "arguments": {"application_id": "$state.selectedApplicationId"}, "dryRun": true, "resultPath": "response.result.application"},
          "releases": {"kind": "mcp", "toolId": "applications.list_releases", "arguments": {"application_id": "$state.selectedApplicationId"}, "dryRun": true, "resultPath": "response.result.releases"},
          "operations": {"kind": "mcp", "toolId": "applications.list_operations", "arguments": {"application_id": "$state.selectedApplicationId"}, "dryRun": true, "resultPath": "response.result.operations"},
          "reports": {"kind": "mcp", "toolId": "applications.list_development_reports", "arguments": {}, "dryRun": true, "resultPath": "response.result.reports"}
        },
        "release_widget": {"type": "ui.list", "inputs": {"itemIdKey": "release_digest", "titleKey": "version", "subtitleKey": "lifecycle", "previewKey": "release_digest", "emptyText": "No versions available."}, "action": {"type": "updateState", "on": "select", "params": {"selectedReleaseDigest": "$event.release_digest"}}},
        "operations_widget": {"type": "ui.list", "inputs": {"emptyText": "No operations yet."}},
        "reports_widget": {"type": "ui.list", "inputs": {"filters": [{"key": "application_id", "stateKey": "selectedApplicationId"}], "emptyText": "No reports yet."}},
        "release_selection": "The Versions list writes selectedReleaseDigest. Install/update plans may leave this state empty to resolve the effective channel.",
        "initial_state": {"selectedReleaseDigest": "", "effectiveReleaseDigest": "", "installationRevision": 0, "subscriptionRevision": 0},
        "lifecycle_settings": {
          "defaults": {"updateTrack": "stable", "updatePolicy": "notify", "removeDataPolicy": "retain"},
          "selector_widgets": [
            {"id": "update-track", "type": "input.selector", "inputs": {"defaultValue": "$state.updateTrack", "options": [{"label": "Stable", "value": "stable"}, {"label": "Prerelease", "value": "prerelease"}]}, "actions": [{"type": "updateState", "on": "change", "params": {"updateTrack": "$event.value"}}], "visibleIf": "$state.applicationInstalled == true"},
            {"id": "update-policy", "type": "input.selector", "inputs": {"defaultValue": "$state.updatePolicy", "options": [{"label": "Notify", "value": "notify"}, {"label": "Automatic compatible", "value": "auto_compatible"}, {"label": "Pinned", "value": "pinned"}]}, "actions": [{"type": "updateState", "on": "change", "params": {"updatePolicy": "$event.value"}}], "visibleIf": "$state.applicationInstalled == true"},
            {"id": "remove-data-policy", "type": "input.selector", "inputs": {"defaultValue": "$state.removeDataPolicy", "options": [{"label": "Retain data", "value": "retain"}, {"label": "Delete data", "value": "delete"}, {"label": "Snapshot then delete", "value": "snapshot_then_delete"}]}, "actions": [{"type": "updateState", "on": "change", "params": {"removeDataPolicy": "$event.value"}}], "visibleIf": "$state.applicationInstalled == true && $state.applicationRemovable == true"}
          ]
        },
        "mutations": "Use separate callMcp actions for install, update, select_track, and remove in one ui.actions widget visible only after an Application is selected. Install/update/remove supply $state.installationRevision as expected_revision; select_track supplies $state.subscriptionRevision. Every plan uses idempotencyKey=auto, resultStateKey=reviewedPlan, and an enabledIf selection guard. A separate applications.apply action is bound to the exact stored operation_id and plan_digest. Never chain plan and apply. Hide remove when applicationRemovable is not true and guard apply until a reviewed receipt exists.",
        "review": "Render one item.details from dataSource {kind:'static', value:'$state.reviewedPlan'} when the stored receipt has an operation id. The user must be able to inspect the exact operation before Apply reviewed plan becomes enabled.",
        "canonical_commands": {
          "buttons": [
            {"id": "install", "label": "Plan install", "kind": "primary", "visibleIf": "$state.applicationInstalled != true && ($state.selectedReleaseDigest || $state.effectiveReleaseDigest)"},
            {"id": "update", "label": "Plan update", "kind": "primary", "visibleIf": "$state.applicationInstalled == true"},
            {"id": "select-track", "label": "Plan track change", "kind": "secondary", "visibleIf": "$state.applicationInstalled == true"},
            {"id": "remove", "label": "Plan removal", "kind": "danger", "visibleIf": "$state.applicationInstalled == true && $state.applicationRemovable == true"},
            {"id": "apply", "label": "Apply reviewed plan", "kind": "secondary"}
          ],
          "plan_actions": [
            {"type": "callMcp", "on": "click:install", "target": "applications.plan", "idempotencyKey": "auto", "resultStateKey": "reviewedPlan", "enabledIf": "$state.selectedApplicationId", "params": {"application_id": "$state.selectedApplicationId", "kind": "install", "expected_revision": "$state.installationRevision", "release_digest": "$state.selectedReleaseDigest", "data_policy": "retain"}},
            {"type": "callMcp", "on": "click:update", "target": "applications.plan", "idempotencyKey": "auto", "resultStateKey": "reviewedPlan", "enabledIf": "$state.selectedApplicationId", "params": {"application_id": "$state.selectedApplicationId", "kind": "update", "expected_revision": "$state.installationRevision", "release_digest": "$state.selectedReleaseDigest"}},
            {"type": "callMcp", "on": "click:select-track", "target": "applications.plan", "idempotencyKey": "auto", "resultStateKey": "reviewedPlan", "enabledIf": "$state.selectedApplicationId", "params": {"application_id": "$state.selectedApplicationId", "kind": "select_track", "expected_revision": "$state.subscriptionRevision", "update_track": "$state.updateTrack", "update_policy": "$state.updatePolicy", "paused": false}},
            {"type": "callMcp", "on": "click:remove", "target": "applications.plan", "idempotencyKey": "auto", "resultStateKey": "reviewedPlan", "enabledIf": "$state.selectedApplicationId", "params": {"application_id": "$state.selectedApplicationId", "kind": "remove", "expected_revision": "$state.installationRevision", "data_policy": "$state.removeDataPolicy"}}
          ],
          "apply_action": {
            "type": "callMcp",
            "on": "click:apply",
            "target": "applications.apply",
            "idempotencyKey": "auto",
            "enabledIf": "$state.reviewedPlan.operation.operation_id && $state.reviewedPlan.operation.plan_digest",
            "params": {
              "operation_id": "$state.reviewedPlan.operation.operation_id",
              "plan_digest": "$state.reviewedPlan.operation.plan_digest"
            }
          }
        },
        "protection": "A protected system Application exposes no remove command in its active installation UI; CLI and MCP remain recovery surfaces."
      },
      "postconditions": [
        "Catalog and installed state come from applications.list rather than static prototype records.",
        "Catalog rows use stable nested Application identity and display projections supported by the renderer.",
        "Selection addresses one exact Application across details, versions, operations, and reports.",
        "Every Root MCP data source is dry-run and mutation-free.",
        "Every plan has an executable operation kind and compare-and-swap revision, stores its exact receipt in visible local review state, and apply is a separate user-reviewed command bound to that receipt.",
        "Compact layouts never display overlapping list and detail surfaces."
      ]
    },
    {
      "id": "recipe.data_entry",
      "title": "Typed data entry",
      "summary": "Capture a typed record and expose explicit lifecycle actions.",
      "aliases": { "en": ["form", "create record", "questionnaire"], "ru": ["форма", "создать запись", "анкета"] },
      "requires": { "layouts": ["layout.flow"], "components": ["ui.form"] },
      "composition": { "input": "Typed fields", "command": "Declared submit or save action" },
      "postconditions": ["Each requested answer has one editable typed field."]
    },
    {
      "id": "recipe.dashboard",
      "title": "Operational dashboard",
      "summary": "Compose independent metrics and summaries in a responsive area grid.",
      "aliases": { "en": ["dashboard", "metrics overview"], "ru": ["дашборд", "обзор метрик"] },
      "requires": { "layouts": ["layout.grid"], "components": ["visual.metricChart"] },
      "composition": { "layout": "Independent peer areas using pattern=dashboard or grid." },
      "postconditions": ["Metric areas use a grid render plan on a sufficiently wide viewport."]
    }
  ]
}
